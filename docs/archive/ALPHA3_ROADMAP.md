# ALPHA 3 ROADMAP — Frazaro / VLA English

*Opened mid-Audit-II at the owner's request, on the microscope-before-dissection principle that has governed the project since Band 0: **earlier items pay structural, harness, evidential, or performance benefit to later items** — the stronger the foundation, the taller the skyscraper without retroactive reconstruction. This file has two halves: a new **Audit VI — the seams** (five boundary instruments a senior review found missing from the five single-perspective audits, each with a named consumer or a named incident), and the **complete ordered sequence** of every remaining unchecked item, carried from ALPHA2_ROADMAP.md with IDs preserved so passes can still be requested by them. Full item definitions, owner decisions, and vetoes stay recorded in Alpha 2, which remains the ledger of record for everything shipped there; this file supersedes only Alpha 2's sequencing section. The ordering below is a proposal, not a decision — the E3 pilot's kill criteria remain pre-committed and binding, and **the standing override still applies: a real user's breakage outranks any line of this document.***

*Ledger note at opening: Audit I (V1–V5) complete and owner-verified; G0 and L0 complete and owner-verified; **L1 (`VlaExpand`) shipped as L1.0, awaiting owner verification and sign-off** — it is recorded in Alpha 2 and not carried here. Nothing in this file wears a checkmark; checkmarks are granted only by the owner's explicit word.*

---

## Part 1 — Audit VI: the seams ("single-perspective review structurally under-produces boundary instruments")

*The five prior audits each instrumented their own layer — the compile gate for VBA, L1/L2 for VLA, G5 for grammar authoring. But the incidents that cost the most (V5.2's phantom dialog, V5.3's topology hole) lived at boundaries **between** perspectives, where no single auditor was looking. These five instruments watch the seams. Each meets the project's bar for existing: a named consumer downstream or a named incident behind it — no instrument-collecting; a microscope nobody schedules time to look through is inventory.*

**✅ S1. The whole-corpus golden diff.** *(Shipped as S1.0 with maiden-run fix S1.1; **verified on-machine and marked complete at the owner's word** — compile clean, self-test passing at +2, goldens generated from `scripts\english.txt` and committed as the baseline. What shipped, pending that verification: **`VlaWriteGoldens`** in VLA_Tests (dev-only, no new module, nothing near the runtime topology) translates `english.txt` through the Run path's own machinery — grammar reset, `spreadsheet.vocab` loaded fresh from disk, step tracking asserted on (the Run state; asserted rather than assumed, since no state getter exists by design) — and writes **`english_golden.vla`** (the complete generated VLA) and **`english_golden.vba`** (its transpile) beside the program. Paths default beside the dev workbook, i.e. into the git working tree; `Application.PathSeparator` throughout (the V5 lesson). The file read is a **rule-12 duplicate** of VLA_English's private `VocabReadFile` (utf-8 via ADODB.Stream, ANSI binary fallback) so the golden translation reads the program byte-for-byte as a Run's import would — sharing the Private cross-module was the rejected alternative, per the self-contained-modules rule. Two self-test pins hold the property the whole instrument rests on: **translation and transpilation are deterministic** (same program twice → identical text at both layers), so a golden diff is always a behavior diff, never noise — the files themselves are proven by `git diff` in the verification loop, which the self-test cannot reach, and the pins prove what makes that proof sound. The verification loop grows one permanent step: run `VlaWriteGoldens` after the self-test; an empty `git diff` on the goldens is the behavior-preservation witness, and a non-empty one is reviewed line by line — **a golden change is a contract change and says so in its commit message**. Recorded for D2: these two files enter the freeze as contract artifacts, exactly as this item predicted. **S1.1 (maiden-run incident, owner-caught):** the generator's default paths assumed the corpus beside the dev workbook; the real layout keeps it in `scripts\` — the instrument that exists to catch unexamined assumptions shipped with one of its own. Fixed by a probing resolver (`FindDevFile`: beside the workbook, then `scripts\`, first hit wins; a miss names both probed locations and the explicit-path escape hatch). No self-test pin — the defect is layout-dependent and no assertion can reach the file system honestly; the regression home is the D3 fresh-machine script, and the *class* of defect is the subject of proposed item S6 below, which this incident surfaced. **S1.2 (owner aesthetic revision — the goldens' first audit finding, one turn after birth):** the first `english_golden.vla` exposed that machine-assembled VLA put closing parentheses on their own lines — block-style, "reads as Java" (the owner's recorded veto of the layout) — while the hand-written step infrastructure was already closer-stacked; only the assembled parts had the habit. Thirteen emission sites re-shaped to traditional Lisp stacking (the at-line wrapper, the If/elseif/else arms, the five loop forms, the three When-case forms, and `BuildSub`'s procedure closer via a new `StackCloser` helper that trims trailing breaks before appending). Safety argued in place: emitted VLA carries no `;` comments and captured raw rows cannot retain one (interior comments force the doesn't-close refusal; trailing ones are never captured), so a stacked closer can never be swallowed. Zero test deltas by construction — every proof comparison (`NormalizeWs`, `Norm`) is layout-blind, and the map pins are hand-written VLA. The consequential side effect, stated honestly: `vla:N` line numbers COMPACT (closer lines no longer exist), so both goldens change — the VLA golden shows the re-layout, and the VBA golden must diff in **`' vla:N` tag numbers only**; anything else in that diff means semantics moved and the pass reverts. This is S1 doing the exact job it was commissioned for, on its first full day: supervising an intentional whole-corpus change line by line.)* "Behavior-preserving" is currently proven by fragment pins plus coarse end-state proxies (vocab count unchanged, VerifyReport unchanged) — a pin only guards where a pin happened to look. *The item:* commit `english.txt`'s complete translated VLA to git as a golden file, regenerate it in every verification loop, and let `git diff` be the total-pipeline witness: a behavior-preserving pass proves itself with an empty diff; a behavior-changing pass shows exactly what changed, everywhere. A second golden of the transpiled VBA extends the same protection over the emitter. Cost near zero — M5a already built the diffing half. *Named consumers:* G1 and G2 are matcher rework, precisely the change class that shifts a translation in an unpinned corner; D2's freeze contract becomes a diffable artifact instead of a test list. **This is the microscope that watches all the other microscopes, which is why it goes first.**

**✅ S2. Rule-usage profiling.** *(Shipped as S2.0; **verified on-machine and marked complete at the owner's word** — compile clean, self-test passing at +8, goldens diffing empty as predicted. What shipped, pending that verification: two counting hooks — every phrase-rule firing at TryPhrase's single commit point (nested firings included), and the claiming structural form once per top-level sentence at `Claim`'s first-wins branch — feeding text-keyed in-memory counters that survive grammar resets and vocabulary reloads, since a count belongs to a pattern, not to a rule index that dies at every reset. **`EnglishRuleUsage()`** returns the report: firings sorted descending (ties keep first-seen order), author-case display over case-blind keys, then the **dead-rule view** — every currently loaded phrase rule with no count, the list G1's deletion review will read. **`EnglishRuleUsageCount(key)`** is the programmatic window (the self-test's, and any future tool's); **`EnglishRuleUsageReset`** starts a fresh measurement. Two recorded design decisions: **vocab `test:` proofs are deliberately not counted** — suspension around `RunVocabTest`, restored on both exit paths — because a proof firing is evidence of loading, not of usage, and without it every tested rule would arrive pre-counted at every load, poisoning the dead-rule view at birth; and **persistence across sessions is deliberately deferred** — the pilot's corpus pipeline (M2a) is the durable home for usage evidence, while this instrument serves the dev loop first, in memory, at zero I/O risk. Eight pins hold the contract: single firing counts once, counts accumulate, the claiming construct counts, proofs don't count, a loaded rule's real firing does, counters outlive a grammar reset, never-fired rules are listed, reset clears.)* The parse-failure log captures what users *couldn't* say; nothing captures what the grammar *actually does* — which rules fire, how often, which never fire at all. *The item:* a counter in the matcher, dumped on demand (`EnglishRuleUsage`), accumulating from the day it lands so every Check and Run thereafter is data. *Named consumers:* G1 merges the rule families that fire and flags the dead ones for deletion review; U2 pins the twenty most-used sentences on evidence instead of taste; the counters name the dead weight in first-full-match scans that V1's measured latency would otherwise only gesture at. The V1 precedent, extended: the grammar gets refined by numbers, not intuition.

**✅ S3. The message seam.** *(Shipped as S3.0 with incident fix S3.1, completion S3.2, and owner revision S3.3; **verified on-machine — compile, self-test, goldens, standalone dialog, and the visual layout smoke — and marked complete at the owner's word.** What shipped, pending that verification: **one seam function for every world** — the design finding of the pass is that V5.3's own architecture already solved the seam's topology problem, because VLA_Runtime is the one module present everywhere (EN_Runtime beside generated code in user workbooks, bundled inside the add-in, loaded in the dev workbook), so `VlaShowError(text, Optional title)` carries the product's voice in all three, with dialog styling (vbExclamation, default title) in one place instead of every call site and every generated program. **`VlaMessageCapture(True)`** flips dialogs into a log (enabling clears it; disabling restores real dialogs); **`VlaCapturedMessages()`** returns them one per line as `title: text` — dialog content as a pinnable artifact, the V5.2 class of defect given a regression home. Converted this pass: the generated step-failure dialog (emitted as `(vlashowerror …)`, S1.2-stacked closers) and `EnglishRunProgram`'s failure handler. **Deliberately NOT seamed: the user's own `Show`/`Say` dialogs** — capture must never eat a dialog the program itself asked for; the distinction is pinned on a single program that carries both. Seven pins total: capture carries text and default title, messages accumulate in order, custom titles honored, re-enabling clears, step infra routes through the seam, the user's Show keeps its `(msgbox …)`, and no raw msgbox remains in the infra. Capture is restored OFF at section end unconditionally, so a dev session after a self-test still gets real dialogs. **S3.1 (first-run incident — and the determinism pin's first capture, eleven days of latent bug, two days of pin):** the owner's first post-S3 suite failed exactly one test — `golden: transpilation is deterministic` — failing on cold sessions (fresh reload), passing on warm ones, reliably. Diagnosis: the C2 source map keys parsed lists by **`ObjPtr` — memory addresses** — and template-copied lists were *keyless* by design ("template statements have no user line"). But an absent key is not an absence: the allocator recycles addresses, so a template copy born at a **dead tagged form's address read the dead form's line** — a phantom `' vla:N` tag that came and went with heap history. Two transpiles of identical text could differ; whether they did depended on session allocation choreography, which is why S3's infra change surfaced a bug as old as the map itself (S3 changed the test program's allocation pattern, not the map). *The fix, semantics-preserving:* "deliberately untagged" became an **explicit stored zero, never an absent key** — `TagLine` stores 0 for line-less lists, `Substitute` explicitly zero-tags every template copy (spliced user forms keep their own tags: the attribution doctrine unchanged), both expansion rebuilds propagate the explicit value, and `EmitStmt`'s existing `srcLn > 0` guard renders zero as "no tag" exactly as before. Every list the module creates now *overwrites* whatever stale entry its address inherited; `FormLine` can never read the dead. One regression pin added beside the hero pin: template statements present in output, never wearing a tag. Deltas from the owner's failing state: self-test **+1** (the S3 ship becomes +8 total over the verified S2 baseline), and on regeneration the goldens may shed phantom tags they were carrying — the committed golden was itself heap-luck; any tag-only diffs beyond the S3 infra region are this fix removing phantoms, and are correct. *The moral, recorded:* S1's determinism pins were justified as "proving the property that makes golden diffs sound" — their first real catch was a heap-dependent attribution bug that every prior audit had walked past, because no instrument could see it. The microscope watched the microscope. **S3.2 (shipped — the seam completed, plus two owner catches from the first standalone dialog):** with VLA_IDE.bas attached, all **twenty** IDE dialog sites now route through the seam (`VlaShowError` for the exclamation class, a new **`VlaShowInfo`** sibling for the information class — same capture, gentler icon), and zero raw `MsgBox` calls remain anywhere in the product's voice; the one `InputBox` (New Program's name prompt) is deliberately not seamed — it is an *ask*, not a message, and gets a scripted-answer hook only if D2's scenarios demand one. *Owner catch #1, branding:* the first standalone dialog said "VLA English" to a user who has only ever met Frazaro — the IDE was rebranded long ago but the engine's old title survived into the seam default. Fixed where such drift goes to die: the seam's default title is now **"Frazaro"**, one place, and every call site passes no title. *Owner catch #2, dialog content:* the step-failure dialog quoted the **parser's lowercased rendition** of the failing sentence — a stranger's paraphrase of what the user wrote. The step table now quotes the **original source line** (case, spacing, all of it; `EnglishToVla` keeps the split source, `SrcLineText` reads it, the parsed rendition remains as a defensive fallback), pinned with a *binary-compare* test because case is the whole point. Deltas from the verified S3.1 state: self-test **+2** (info capture; original-sentence quoting), one pin updated (default title → Frazaro); goldens diff **only** in the `vla-step-text` literals gaining their original case; VerifyReport and vocab unchanged. The D2-shaped Undo-content pins ("names only real sheets") remain D2 scope as planned — the capture hook they need now exists, which was this item's whole obligation. **S3.3 (owner revision — file/sheet line parity):** the program sheet no longer steals row 1. `FIRST_ROW` is 1; the old A1 guidance line lives in a **cell comment** on A1; the "Result" header is gone (column B still marks per-row results beside their rows); C1 keeps its small grey role as the optional per-sheet vocab path — it sits in an otherwise-unused column and offsets no line numbers (owner may veto its placement). Sheet row N now IS program line N, matching the file a program would save to — the `[line N]` in every dialog, the red-cell jump, and the log's row column all agree with what the user sees. **Legacy sheets migrate in place exactly once**, keyed on the old `B1 = "Result"` marker: the C1 path is kept, row 1 is deleted (program and its column-B marks slide up into parity together), and the path is restored — the migration runs at Check and at the Import/Reload chokepoint, so no route can write at the new `FIRST_ROW` while the old header still sits above it. Untestable by self-test honestly (sheet surgery); the verification loop and D3's scenario table are its regression home.)* The self-test "cannot reach MsgBox-bearing paths," so half the product — the error voice, the dialogs — is tested only by the owner's eyes. *The incident:* V5.2 — the Undo dialog named five phantom sheets beside one truth for the entire project history, because no instrument could read a dialog; it took a real user reading a real screenshot. *The item:* route every user-facing message through one function with a capture mode the test harness can flip, making dialog **content** pinnable — the Undo dialog names only real sheets, the step-failure dialog names its sentence, refusal wording cannot drift. *Named consumers:* U3 proposes building **more** dialog surface (readable error boxes, healing flashes) — build the harness that can see dialogs before building more dialogs; D2 stops deferring dialog-truth scenarios to manual D3 clicking. Soft-failure doctrine applies: the seam is auxiliary machinery and fails softer than the message it carries.

**✅ S4. The pre-run compile probe → re-scoped: the resolve check.** *(**Verified on-machine and marked complete at the owner's word** — compile, self-test through the S4.2 pins, the live typo'd-helper refusal, and the replayed sheet-incident gate. Two owner decisions recorded at closing: the `in`/`into` cell-rule gap surfaced during verification is **deferred to G1 as its first customer** (see G1's note in Part 2 — no vocab edit now, the 181 baseline stands), and **`Go to sheet` / `Work on sheet` stay distinct verbs** — go-to fails on a missing sheet by design, work-on creates it; collapsing them would delete a safety affordance, and S2's usage counters are the appointed evidence if pilot users keep tripping. S4.0 shipped the sacrificial-stub probe; **S4.1 removed it after the owner's deliberate-defect test disproved the mechanism on a real machine** — the rare and valuable outcome where a ten-minute test kills a design before it ships false confidence. The empirical verdict, recorded in full because it is reusable VBA knowledge: **(1)** VBA's default *Compile On Demand* compiles per **procedure**, not per module — the sacrificial no-op compiled and verified nothing beyond itself, and a defect planted in a sibling procedure raised no error at all; **(2)** when the defective procedure *was* forced to compile under `Application.Run`, the error crossed the project boundary as **VBA's untrappable modal**, not as a trappable `Err` in the calling handler. Fact (2) kills the entire in-VBA probe family, not just the shipped variant — any cleverer design (e.g. `AddressOf`-touching every generated procedure to force compilation) still uses VBA's compiler as the detector, and the compiler answers with a modal. S4.1 reverts the probe emission, the Run-path call, and both pins (self-test **−2**, back to the verified S3.3 total; goldens shrink the probe region and diff nowhere else). **The re-scope, awaiting owner direction:** the real-world failures this item exists for are, ranked honestly — a dialect template naming a helper that doesn't exist (V5.3's exact class, multiplied by every L4 phrasebook author, who has no compile gate); F2's LLM-drafted programs; a copied workbook with a hand-damaged `EN_Runtime`. Nearly all are **name-resolution** failures — and detecting those needs no VBA compiler: we generate the text and we know the full universe of resolvable names (the module's own procedures, `EN_Runtime`'s publics, the emitter's VBA vocabulary). The re-scoped item is a **static resolve check** at Check/Run time: scan the generated code's call targets against that universe and refuse with a teaching message that *names the missing helper*, before anything is injected — modal-free by construction, fully self-testable (pure string work, unlike the probe), and better UX than the probe even in the world where the probe worked. Recorded residual risk: pure structural malformation reaching a user stays guarded dev-side (the compile gate, S1's VBA golden) and, if it ever fires in the field, still ends at VBA's modal — the honest limit of what an add-in can intercept. Natural slot: immediately before **L4**, its chief consumer; implementable on request as one modest pass.)* **S4.2 (shipped at the owner's word — the resolve check, awaiting verification):** `VlaHelperManifest` in VLA_Runtime self-scans `RuntimeSourceText` for every Public vla-name (maintenance-free in both source tiers: live module or the build's `VLAr_Source` sheet); `EnglishResolveCheck` scans the translated VLA in two passes — definitions first, then every call head — and any **vla-prefixed** name resolving to neither the program's own subs/functions nor the manifest refuses **at Check**, marked at its exact row (the `(at-line N)` markers thread the line, and line IS row since S3.3), with a message that names the helper and points at the vocabulary's maintainer. String literals are skipped by a string-state scanner; hyphen/underscore/case spellings unify; an empty manifest means "cannot verify" and passes (soft-failure: auxiliary machinery fails softer than the Run it guards). Non-vla names deliberately pass — VBA builtins and user words belong to VBA's judgement, the recorded residual. **Unlike the probe, this is pure string work, so the self-test holds the whole contract: seven pins**, including a live end-to-end (a real tracked translation resolving its own infra's `vlashowerror` against the real manifest). Deltas from the verified S4.1 state: self-test **+7**, goldens unchanged (nothing emitted changed), vocab and VerifyReport unchanged. **S4.3 (owner incident, same session — workspace sheets are not output sheets):** `Work on sheet Frazaro.` aimed the Run at the program sheet itself — a "protected" step failure (the guard that worked), then a stray visible `Frazaro (2)` (the snapshot's copy of the program sheet, its rename to the `VLAu_` slot evidently failing on the wreckage), no Output sheet (correct, given the declaration), and an Undo dying with a bare "Automation error". Root cause gated where the owner proposed: **one Check-time refusal** — `ForbiddenSheetTarget` scans the sheet's rows for `Work on sheet` and `Go to sheet` targets and refuses, at the offending row, any name that is a workspace sheet, the log/phrases/feedback sheet, or a `VLAu_`/`VLAd_` snapshot name — killing the whole downstream class (snapshot corruption, stray copies, wounded Undo) at the gate instead of patching each symptom. The Undo failure's bare branch also gained context ("Undo stopped before it could change anything… a leftover VLAu_ sheet or stray program-sheet copy can cause this; deleting it clears the way") — the "Automation error" itself was Excel's whole story at that layer, so the honest improvement is where it died and what clears it, with deeper phase forensics waiting for demand. The gate is sheet surgery the self-test cannot reach; the verification loop and D3's scenario table own it. The Audit-I closing note's open finding, promoted from candidate to item: VBA compile errors in the generated module are untrappable by `On Error`, so a defective `EN_<program>` module drops a non-developer into the VBE — the exact experience the error voice exists to prevent (surfaced during the V5.3 saga). *The item:* a Check/Run-side compile probe of the generated module before execution, catching structure-class defects at the user's boundary the way Debug → Compile catches them at ours. This is the dev-side compile gate — the instrument that has caught more blind-writing bugs than any other — installed where users live. *Named consumer:* the pilot; no stranger meets the VBE. Scope honestly at implementation: the probe's own mechanism (a `VBIDE`-level compile touch vs. a sacrificial `Application.Run` against a stub) is undecided and gets the usual veto-invited design note.

**✅ S5. The runtime trace.** *(**Verified on-machine and marked complete at the owner's word** — Run & Trace produced the Trace tab through the front door, the ribbon-contract pin passes at ten, and the full suite is green. **AUDIT VI IS CLOSED**: all five seam instruments shipped and owner-verified; S6 remains a proposal awaiting adoption. **One open finding, BACKLOGGED at the owner's word (the session's parting bug):** clicking Run on an empty program sheet still surfaces Excel's raw "Cannot run the macro '…EN_Sheet.main'" box and an empty Output tab — despite two guards (top-of-path empty check, `ModuleHasMain` before `Application.Run`) that both simulate correctly. Three blind fixes failed, which is the diagnostic-first lesson: the next session must MEASURE before patching. The recorded protocol, verbatim for pickup: **(1)** with the empty sheet active, in the Immediate window run `?ActiveSheet.Name, ActiveSheet.Cells(ActiveSheet.Rows.Count,1).End(-4162).Row` and `?"[" & ActiveSheet.Cells(1,1).Value & "]" & "[" & ActiveSheet.Cells(2,1).Value & "]"` — sheet identity, what `IdeLastRow` returns, whether A1/A2 truly read empty; **(2)** name WHICH control was clicked (sheet button / ribbon / menu — they may not share a route); **(3)** confirm a non-empty Run still works, proving `RunProgram` is the live compiled path. Leading suspects, in order: the click routing to something other than `RunProgram`; A1 carrying invisible residue (possibly from the S3.3 row-1 migration); an un-reloaded module. Fix only after the readouts.)* **S5.6 (standing-override field report, post-checkmark — honored per doctrine):** the first *built-add-in* Run & Trace on a fresh workbook recorded nothing, and the diagnosis is the V5.3 lesson restated for STATE: `RunProgram` executes in the add-in, so its `VlaTrace True` armed the **add-in's** seam instance, while `main` resolves `vlatraceon` in-project to the **injected EN_Runtime's** instance — two copies, and the steps consulted the cold one. The dev workbook masked it by construction (the same-project guard skips injection, so one instance served both roles), and the S5-era claim that "the S3 standalone test already proved the plumbing" was wrong in a way now recorded as discipline: **stateless linkage proving is not stateful sharing proving** — S3's dialogs are stateless per call; the trace is the seam's first stateful citizen, and state is exactly what the standalone check exists to test. *The fix:* `ArmTrace hb` flips the trace **on the instance the run consults** — host-qualified through `Application.Run "'…'!EN_Runtime.VlaTrace"` (no VBProject trust needed), with the local instance as the dev-topology fallback (where no EN_Runtime exists, the qualified call fails, and local IS correct — the fallback is the design). All three call sites converted, including the failed path's wrong-instance `wasTracing` read. Cross-project by nature, so no unit pin can reach it: **the standalone re-run is the verdict** — rebuild the add-in, fresh workbook, Run & Trace, expect the transcript. *(Shipped as S5.0 — **awaiting owner verification and sign-off**; expected deltas from the verified S4.3 baseline: self-test **+6**, vocab unchanged 181, VerifyReport unchanged H1:H31 (the trace defaults OFF, so a normal Run pays one Boolean check per step and behaves identically); **the goldens change everywhere, deliberately and reviewably** — every tracked step gains one guarded line, `(if (vlatraceon) (then (vlatracestep N (vla-step-text N))))`, between its step marker and its sentence, so both goldens diff as a uniform per-step addition and nothing else; `' vla:N` tags are UNCHANGED, since they carry user-source lines, not VLA text lines. What shipped, pending that verification: the trace seam lives in VLA_Runtime **above the inject boundary** — the S3 lesson applied by reflex, since generated code calls it in every topology — with `VlaTrace(True)` clearing and arming, `VlaTraceStep` logging each step's number and **original sentence** (S3.2's text, reused) in execution order — loop passes appear once per pass, which is the point — capped at 20,000 entries so a runaway loop cannot eat the session, and `VlaTraceReport()` returning the numbered transcript. `VlaTraceOn()` is the guard the generated code reads, so the off-cost is one flag check per step, forever. Six pins hold the emission contract (every tracked step carries the guarded call; it survives to VBA under runtime names) and the seam's semantics (execution order with text, re-enable clears, off means silent, the guard reads the flag); the S4.2 resolve check's live pin now also proves, for free, that the four new runtime names resolve through the self-scanning manifest — three instruments verifying each other unprompted. The full Run-with-trace is the verification loop's smoke: `VlaTrace True` in the Immediate window, Run english.txt, `Debug.Print VlaTraceReport()` — the corpus's whole execution, loops unrolled, as numbered sentences. **S5.1 (owner smoke-test catch — the session's second empirical kill, and the smoke step's first):** the first traced Run recorded nothing, and the mechanism is a lesson worth its own sentence: **the Run itself wipes the flag** — injecting `EN_<program>` into the project that hosts its own run (the dev topology) resets every module-level variable in that project before `main`'s first step, so a flag flipped in the Immediate window was dead by the time the first guard read it, and the report then read a fresh, empty log. VBA project state loss on self-modification is the same force that made D1 capture the host handle; it had simply never been aimed at the seam's own state before. *The fix:* the flag's durable half is a hidden **workbook `Name`** (`VLAt_TraceOn`) — Names are workbook *data*, not VBA state, and survive recompiles — with the module cache preserving the one-Boolean-check per-step cost: state loss clears the cache-loaded marker, which is precisely the reload signal, so the first guard after any recompile reads the Name once and re-caches. `VlaTraceStep` now guards through the reloading path too. In the injected topology, `EN_Runtime`'s `ThisWorkbook` is the *user* workbook, so tracing a standalone run means flipping *its* copy — `Application.Run "'Book1'!EN_Runtime.VlaTrace", True` — documented at the seam. Two pins hold the durable half (the Name reads `1`/`0` after flip); the cache-reload path is exercised for real only by an actual Run, so **the re-run of the smoke is the true verdict**. Deltas from the failing state: self-test **+2** (S5's ship becomes +8 over the verified S4.3 baseline); goldens, vocab, VerifyReport unchanged by the fix. **S5.2 (maiden-compile incident):** S5.1 placed `TRACE_NAME` — a module-level `Const` — beside its machinery, mid-module, drawing the exact error the D1.1 rule exists to prevent ("Only comments may appear after End Sub…"), the rule's second victim in this project and one memorialized in VLA_IDE's own comments. Relocated to the declarations block; a project-wide sweep confirms zero other module-level declarations sit below any file's first procedure. Caught by the compile gate on the owner's machine — the one instrument the blind-writing workflow cannot run itself, which is why Debug → Compile is step one of every loop. **S5.3 → S5.4 (owner design, replacing the durable-flag approach — the trace's real home):** the Name-backed flag still failed the smoke because the two remaining failures were never about durability — they were about *topology and timing*. The Immediate-window `VlaTrace True` set the **add-in's** flag, while `main` (running in the user workbook) read **`EN_Runtime`'s** copy: two `ThisWorkbook`s, never the same flag. The owner's design dissolves it: a **`Run & Trace`** command arms the trace *after compile and in the same execution chain as the run*, so no state crosses an idle boundary or a project reset — the failure both prior attempts lived in simply has no gap left to occupy. The transcript pours onto a **`Trace` sheet** (the Feedback-sheet pattern: Ctrl+A, Ctrl+C, paste into an email) read back through `EN_Runtime` so it is topology-correct standalone, and a *failed* traced run still shows the steps it reached — often the whole diagnosis. `RunProgram(wantTrace)` is the shared core: tracing arms `VlaTrace` around `main` only, so it can never change what a program does, only whether steps are recorded. The Immediate-window `VlaTrace`/`VlaTraceReport` API stays alive underneath as **F1's parity-harness interface** — the button is the human front door to the same seam. Wired in both surfaces per the owner's spec: the IDE sheet button between Run Sentences and Undo Last Run, and the ribbon (`Run with Trace` — ampersand-free, since customUI labels are XML and `RibbonBtn` does not escape; the sheet button keeps `Run & Trace`, a VBA caption where `&` is literal). **S5.4 completed a partial S5.3 that had shipped three defects** the compile gate and a code read caught together: a `Dim` welded onto `RunProgram`'s signature line (a module-wide compile failure — which is why the trace never armed: the old `EnglishIdeRun` was what actually ran), a ribbon dispatch missing its `VlaRunTrace` case, and ribbon XML with no button at all. **The empty-run dialog** the owner hit — Excel's raw "Cannot run the macro '…EN_Sheet.main'" when an empty program reached `Application.Run` with no `main` ever generated — is fixed at the *top* of the Run path (guard before any furniture, in Run's own voice: "No sentences to run — write a program in column A first"), not as a bottom-of-path dialog swap. Deltas: self-test unchanged (Run-path behavior is IDE territory the self-test cannot reach — the verification loop and D3 own it; stamps bumped for coherence). **`VLA_Build.bas` now in the reload set for this pass.** **S5.5 (owner-caught, two findings):** the self-test flagged `ribbon: every button rides the one callback - got 10` — a *correct* catch, the ribbon contract test still expected 9 buttons after S5.4 added the tenth (`VlaRunTrace`); the id list and both counts now expect 10, the instrument doing exactly its job. And the empty-Run box persisted: the top-of-path guard is sound (it refuses every whitespace-only sheet), so the screenshot was the pre-S5.4 module — whose welded-`Dim` compile failure meant `RunProgram` and its guard never ran at all; the old `EnglishIdeRun` did. Rather than rely on "recompile fixes it," S5.5 adds **defense in depth**: `ModuleHasMain` confirms the freshly compiled module actually defines `main` (a comments-only program translates to a bodyless module) immediately before `Application.Run`, refusing in Frazaro's voice with nothing touched if it does not — so the raw "Cannot run the macro" box cannot reach a user by any path, present or future. Best-effort by doctrine: if the check cannot read the project it returns True and lets the run proceed, no worse than before it existed. Self-test **+0** net (the failing ribbon pin now passes; no new pins — the guard is Run-path territory D3 owns).)* The run is observable at exactly two points: the failing step (attribution) and the end state (VerifyReport). Nothing observes the middle of a *successful-but-wrong* run. *The item:* the step handler — already 90% of a trace — gains a mode that logs every step it passes instead of only catching the fatal one, dumped as sentence-numbered lines. *Named consumer:* F1's parity harness is the killer application — when interpreter and transpiler disagree, VerifyReport says "H13 differs" and leaves a bisection; a trace from each mode, diffed, names the first divergent step in one run. Also the general instrument for "it ran green and did the wrong thing," which is U5's report shape arriving before U5 exists. *Honesty note, recorded:* S4 and S5 have distant named consumers (pilot, F1); front-loading them here is the owner's microscope-first call, and deferral to their consumer's band is the sanctioned fallback if capacity demands — the recorded reason they sit fourth and fifth inside their own band.

**⬜ S6. The path seam — files are configuration, not assumptions.** *(Proposed at the S1.1 incident, owner-prompted; **adoption, scope, and banding are the owner's call** — the reasoning is recorded here per protocol rather than left in chat.)* *The incident class:* the project's two external dependencies — phrasebooks and program files — are located today by scattered per-tool knowledge (the IDE's vocab path, the goldens' probe, E2's planned `scripts\*.vocab` glob), and S1.1 proved the failure mode ships even in tools built the same week. *The proposal, four parts with recorded dispositions:* **(1) One resolver.** File-location knowledge moves to a single workbook-scoped configuration (the V2 state-row pattern — per-workbook, visible to Diagnostics, no registry, no hard-coded subfolder); the engine stays path-agnostic as it already is (`EnglishLoadVocabulary`/`EnglishRunProgram` take paths — correct layering, preserved), the IDE owns resolution, and dev tools ask the same convention. Program files selectable from anywhere on disk via a real picker — the `scripts\` folder becomes a default, never an assumption. **(2) The phrasebook manager** — the owner's Name-Manager analog, accepted and endorsed: a dialog listing loaded phrasebooks *in load order* with per-file rule counts, add/remove/reorder, and "which file won this rule" (G3's Explain feature surfaced as UI). **Sequencing constraint, firm:** the manager ships *with or after* G3's override semantics, never before — a multi-phrasebook UI atop silent first-full-match shadowing is a footgun-arranger, the exact hazard G3 exists to end; this makes the manager the natural centerpiece of E2's multi-phrasebook loading. **(3) Workbook-embedded phrasebooks, the strengthening:** imported phrasebooks copy into the workbook itself (very-hidden sheets, the proven `VLAr_Source` pattern), with the source path remembered for Reload — making a workbook fully self-contained: program + phrasebooks + `EN_Runtime`, traveling as workbooks actually travel (email, network shares), dissolving the moved-file and missing-folder failure classes outright and extending V5.3's self-containment dividend to the grammar layer. Files on disk become import/export interchange, not runtime dependencies. **(4) Remote URLs — vetoed as live-fetch, with reasons:** a phrasebook defines what sentences *do* — its templates emit VLA that becomes VBA — so fetching one from a URL at load time is remote code inclusion by another name, and it detonates M1a's entire security posture (deterministic, offline, auditable, easiest macro tech to whitelist) plus the reproducibility that makes golden-style reasoning possible per org (a Run's meaning would depend on a server's mood). The accepted shape of the same wish: **download → review → import** — a file the user possesses passes through the manager like any other, and F3's versioned releases plus G3's governance remain the sanctioned distribution channel (which M2b prices). *Naming, recorded recommendation:* `.vocab` → **`.phrasebook`** — self-describing, wears the brand, and *functionally* motivated: E2's glob-loading needs an extension that cannot swallow a stray `README.txt`, and the M1a one-pager gets to say "`.phrasebook` files are plain text" in one line. Do it **before** E2/D2 while no external file exists (the V3.2 lesson: clean renames are cheap exactly once). The program-file extension (`.txt` vs `.english` vs `.frazaro`) is *deliberately deferred* to E1's pilot-evidence window alongside the comment-syntax decision — same decision family, and the double-click-into-Notepad friction question needs real users to answer (programs live primarily in the sheet and arrive via Word import; whether users exchange program *files* at all is pilot data).

---

## Part 2 — Every remaining unchecked item, ordered microscope-to-dissection

*One line of principle, inherited and extended: **instruments before library, library before surface, surface before freeze, freeze before strangers, trust before the pilot, the pilot before demand-ranking.** Each band pays a stated benefit to the bands after it; reading the bands backwards is the dependency proof. Items land with their Alpha-2 IDs and a one-line reminder — the full definitions, suggestions, and recorded decisions live in ALPHA2_ROADMAP.md.*

**Band 0 — The seam instruments** *(everything below debugs through these; all five are cheap relative to what they watch).*
1. **S1** — the whole-corpus golden diff (VLA golden + VBA golden in git). Pays: every subsequent pass proves behavior-preservation mechanically; G1/G2's matcher rework lands under total surveillance; D2 freezes a diffable artifact.
2. **S2** — rule-usage profiling. Pays: starts accumulating evidence immediately, so by the time G1 and U2 need it the data exists; names dead rules before the freeze contracts them.
3. **S3** — the message seam. Pays: every dialog and refusal written after it is born testable — which covers all of G2/G4's teaching refusals and U3's new surfaces; retires a whole class of V5.2-shaped invisible nonsense.
4. **S4** — the pre-run compile probe. Pays: closes Audit I's open finding before any pass widens the generated-code surface; the pilot's floor.
5. **S5** — the runtime trace. Pays: successful-but-wrong runs become observable; F1's parity harness inherits its instrument fully built.

**Band 1 — VLA layer completion** *(Audit II's remainder minus L4, which is grammar-facing and waits for its machinery; the layer stack's second story finishes before the third begins).*
6. **L2** — `VlaTry`, the scratch evaluation loop (temp Sub, D1-threaded compile, scratch module, run, delete; dev-rig only, never in the add-in; L0's raw rows already cover the in-sheet half). Pays: every prelude and engine pass below iterates in seconds instead of program-edit cycles.
7. **L3 + L5** — grow the prelude (`let-one` family, `if-not`, `while-not`, `(comment …)`) and document the template-macro limits and idioms in VLA_GUIDE, including the "can this be a template?" test. Verified through L1's `VlaExpand` — the instrument paying for itself first, exactly as Band 0 logic predicted. Pays: the trapdoor line ("templates only, never smooth VBA semantics") gets a published test that keeps every later macro request honest, L4's phrasebook macros included.

**Band 2 — Grammar infrastructure** *(the language and phrasebook surface finishes before the freeze, so D2 contracts something complete; ordered so each matcher/loader feature feeds the next; the whole band lands under S1's diff and S2's counters).*
8. **G1** — alternation slots (`{d:left|right}`) and optional literals; audit learns to expand alternations. Every rule written after this is cheaper; S2's counters name which existing rule families it collapses. **First customer, pre-registered (S4-closing decision):** the `put … {into|in} cell` family — `Put 1 in cell A2.` currently mis-refuses with a status-bar near-miss because only `into` rules exist, and `in` is too loaded a grammar word (`in range`, `in column`, `in {d:var}`, `in status bar`) for a blanket synonym; the alternation slot is the exact tool, applied to the three `put … into cell` rules and the computed `into column … row` variants, each with `test:` lines. G1 does not ship without this family converted and `Put 1 in cell A2.` translating.
9. **G2** — typed reference slots (`:range`, `:cell`, `:sheet`, `:color`) with Check-time teaching refusals — and the machine-readable "what does this rule touch" data U1's Rehearse consumes one band later. Refusal wording pinnable via S3.
10. **G4** — the `fail:` directive: refusals become load-provable inside phrasebooks, not only in the dev-only self-test; dialect authors can protect their own refusal decisions.
11. **G3** — dialect override semantics (`override:` marker, load-order contract, Explain names which file won). Must precede E2's multi-phrasebook loading or load order becomes accidental precedence.
12. **L4** — the `macro:` directive: phrasebooks carry `defmacro`s, making dialects complete extension surfaces — audited and testable like everything else, refusal-shaped tests on G4's machinery, expansions inspected through L1 (the instrument that makes shipping this surface to non-engine authors responsible at all).
13. **G5** — `EnglishTryRule`, the rule scratchpad — last in the band so it auditions the *finished* authoring surface (alternations, typed slots, overrides, macros); E2's starter phrasebooks are then written with it.

**Band 3 — Freeze, trust, and ship prep** *(the gate sequence before strangers touch it).*
14. **D2** — freeze + compatibility contract over the completed surface: G4 refusals in the contract, V4 attribution in the contract, marker-free block-form vocab tests, build-time assertions, the D1.2 tombstone promise as a D3-testable scenario — and, new: S1's goldens as contract artifacts, S3-reachable dialog content in scope.
15. **U1** — **Rehearse**: the static "this Run would touch…" preview, consuming G2's typed slots; dangerous verbs listed first. The trust feature a stranger meets first; the one item licensed to slip into the pilot window if capacity demands.
16. **U2 + U3** — Known Sentences categorized and searchable (S2's most-used data pins the top twenty); check-on-row-exit and readable error dialogs (on V1's speed, through S3's seam).
17. **E2** — first-contact polish: New Program seeding a commented example, multi-phrasebook loading (legal now that G3 exists), starter phrasebooks authored via G5.
18. **M2a + U5 groundwork** — the consented corpus pipeline and the maintainer-address config; U5's "that's not what I meant" reuses U1's behavior summary (and S5's trace shape), so its plumbing lands here and its button ships in pilot week one.
19. **M1a + F3** — the IT security one-pager (deterministic, offline, auditable — the easiest macro tech to whitelist, in writing) and code-signing / versioned phrasebook releases.
20. **D3** — rebuild, lock, fresh-machine test: the D1 scenario table plus the tombstone Undo plus the **closed-dev-workbook standalone run as headline** (the V5.3 lesson) plus the V2 two-program interleave, with **M5b** folded in — run it with an actual stranger, and the hit-by-a-bus drill (repo + LESSONS.md → one vocabulary rule shipped unassisted).
21. **E1 (remainder)** — trademark/domain checks (parallel, no dependencies); the comment-syntax decision stays *deliberately deferred into the pilot* for parse-failure evidence.

**Band 4 — The gate.**
22. **E3** — the pilot: three to five power users found by artifact; pair-write the hated weekly task; kill criteria pre-committed in writing and binding — (1) one program still clicked after a month of Mondays, (2) one user modified a program alone; both fail → the pre-chosen pivot. During the month: U5's button ships week one, **M4**'s beachhead and maintainer-plus-seats pricing validation runs through the interviews, E1's comment syntax is decided on the log's evidence — and S2's counters plus the corpus become the re-ranking input for everything below.

**Band 5 — Parallel with the pilot** *(neither blocks nor waits on it).*
23. **F1** — interpreter mode, derisking per M1, not a feature: executor walking parsed VLA, no VBProject trust setting, plain `.xlsx`, the Mac path. `english.txt` identical under both modes is the definition of done; `VerifyReport` is the parity harness, and **S5's diffed traces** are what make a parity failure a one-run diagnosis instead of a bisection.
24. **F2** — the LLM-draft bridge (M3's answer made shippable): V1's fast Check is its inner loop, G2's teaching refusals make its retries converge; publish the defect-rate number the thesis lives on.

**Band 6 — Demand-ranked by the corpus** *(the order below is the prior; the pilot log overrides it, item by item).*
25. **U4 / Tier-2 helpers** — charts and conditional formatting first (the predicted top asks), then dropdowns, pivots, regex, web requests — each a `VlaColor`-pattern helper behind a G5-auditioned verb, landing in `VLA_Runtime` per the V5.3 topology pins.
26. **The Tier-2 ladder** — the accepted dev-rig `(class …)` spike, then VLA Tier 2 (classes, `defprop`, the multi-component compile contract on D1's captured handle), then **events** as the flagship, then **self-hosting the loader** — parity-validated, and every instrument above (L1's `VlaExpand`, L2's `VlaTry`, S1's goldens, S5's trace) pays for itself again here, which was Band 0's original wager.
27. **M2b and the horizon** — dialect governance as the enterprise SKU (on G3's override machinery + review workflow), localization (the grammar is data), macro-recorder-to-English; Dictionary-representation and matcher indexing promoted only when S2's counters and V1's printed latency say so, not when vocabularies triple.

*The dependency proof, read backwards: self-hosting needs Tier 2, which needs the spike and D1's handle; the pilot needs D3, which needs D2, which is only worth freezing after Bands 1–2 finish the surface; Rehearse needs typed slots; typed slots need the matcher work; the matcher work is only safe under the golden diff and only evidence-led under the counters; the counters, goldens, seam, probe, and trace cost an afternoon each precisely **because** the machinery they instrument is open right now. Microscopes first — then the dissection is quick, and nothing bleeds unobserved.*