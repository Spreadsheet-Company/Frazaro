# Alpha 5 Roadmap — Performance, Lisp, Grammar, UI

*Drafted at the close of the Band-1/G-line session. Verified baselines: self-test **357** / 0 failures; vocabulary **101 rules / 199 tests / 5 fails / 16 macros**; goldens committed; stamps VLA core L3.0, VLA_English G10.0, VLA_Runtime S5.2, VLA_Tests G11.0, VLA_DevRig V6.2, VLA_IDE S5.5, VLA_Build S5.4. **Alpha 2 remains the ledger of record for D/E/F/M/U definitions; Alpha 4 holds the L6–L15 definitions, the G6–G9/V7–V8/L16–L17 proposals, and the gathered vetoes** — this file recomposes the unfinished engineering work into the owner's four buckets, each ordered microscope-before-dissection, worked **in bucket order: Performance → Lisp → Grammar → UI, beginning with V7.** The ship-sequence business track (D2→D3→E3 pilot, F1/F2, M-items) is carried at the bottom by reference, not dissolved — the buckets feed it; they do not replace it.*

**Bucket-entry rule (Performance):** the bucket does not open until a `VlaTimeIt` reading is pasted to the ledger — V6 shipped and verified, but its maiden four numbers were never recorded, and **V7 requires the before-number.** That paste also formally closes the backlogged empty-Run finding.

> **Before-number recorded (session paste, opens the bucket for V7):** `VLA-IDE: Check timing - vocabulary 86 ms, translation 94 ms, 303 rows` — the IDE's translate-once Check line, pre-V7, covering exactly the two legs V7 targets. Bookkeeping kept honest: this is the Check line, **not** `VlaTimeIt`'s four dials — the transpile and Run legs still have no before-number, so (a) **V8 does not open** until the four-dial printout lands (its string pass touches the transpile leg), and (b) the **empty-Run finding stays open** until the Run dial is pasted (that finding is about the inclusive Run number specifically). Owner's stated plan stands: activate the english.txt workspace workbook (the one with the Output sheet) and run `VlaTimeIt` last in the session — that one paste supplies V7's after-numbers, V8's before-numbers, and the finding's closure together.
>
> **Four-dial `VlaTimeIt` baseline PASTED (pre-V7, run before compiling the V7 files) — the bucket-entry rule is now fully satisfied:** vocab load **91 ms** (prev 227), translate **49 ms** (prev 76), transpile **388 ms** (prev 393), run (full, inclusive reset+load+translate+inject+execute) **641 ms** (prev 609) — stored in workbook names `VLAt_Time*`, which is where V7/V8 read their before-numbers per the dial's own note. Consequences on the ledger: **V8 is now open** (transpile leg has its before-number: 388 ms) and the **backlogged empty-Run finding is formally CLOSED** — the Run dial exists, reads 641 ms inclusive, and the dial's printout itself confirms the Run leg executed `english.txt` for real (owner note attached to the paste: *reload the vocabulary before the next Check*, since the timing run consumed a real load). The `prev` column is the previous stored run's values, recorded verbatim without interpretation. **Instrument discipline:** the Check line (86/94) and `VlaTimeIt` (91/49) measure different scopes — translate 94 vs 49 ms are not the same dial — so after-numbers must be compared like-for-like: Check-vs-Check, TimeIt-vs-TimeIt, never across. V7's after-numbers: the four dials re-run after V7 verifies, against 91/49/388/641.

---

## Bucket 1 — PERFORMANCE *(the dial exists; now the two passes it gates)*

- **✅ P.1 — V7: first-token rule dispatch — VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(shipped as V7.0; compile clean, self-test 367/0, all tests passed in BOTH the dev workbook and a fresh workbook — the owed standalone-topology check satisfied — goldens as predicted. After-numbers, dev-workbook `VlaTimeIt` against its own stored prev column: vocab load **69 ms** (prev 104, −34%), translate **38 ms** (prev 55, −31%), transpile **385 ms** (prev 396, flat as designed — V7 never touched that leg), run (full) **582 ms** (prev 594). Fresh-workbook initial Check line: `vocabulary 164 ms, translation 51 ms, 303 rows` — translation 94→51 against the pre-V7 Check line; the 164 ms vocabulary figure is a COLD first load in a different venue and is recorded verbatim, not read as a regression — instrument discipline says Check-vs-Check same-venue warm-vs-warm before concluding anything; a warm re-Check there would settle it if the owner cares. Verdict: both targeted dials dropped about a third at today's 101 rules; the real purchase is the scaling law — translate cost now rides bucket size, not vocabulary size.)* *(definition on Alpha 4).* The microscope came first (V6); this is the dissection. Bucket rules by first matchable token — literals, bare and braced alternation branches expanded, surface forms expanded, "slot-first" as its own bucket — into a keyed index rebuilt at load; sentence dispatch jumps to its bucket. Behavior-invariant by construction: **the goldens are the witness (both must diff empty), the 199+5 proofs are the gate, and V6's before/after pair is the proof of gain.** Design note carried from G10: the index must expand surfaces exactly as `ExpandedSignatures` does — reuse `BareSurfaces`/`SurfaceForms`, never a parallel expansion.

  > **V7.0 record (shipped blind — the machine has not spoken).** Touched: `VLA_English.bas` (→ V7.0), `VLA_Tests.bas` (→ V7.0). **The design as built:** a keyed `Collection` of buckets (`token → ascending rule indices`) plus a *universal* list for rules whose first non-optional item is a value slot (any token can begin them, so they are always tried for real). Per rule, the first-token set is computed by walking the pattern exactly as `TryPhrase` would at position zero: leading optionals contribute their surfaces **and fall through** (free when absent, so the next item also begins the rule); the first non-optional item decides — literal → itself, bare or braced alternation → every surface of every branch **via `BareSurfaces`/`SurfaceForms`, the same expansion the signatures ride** (the G10 design note honored; no parallel expansion exists); an all-optional pattern is conservatively universal. Dispatch reads the sentence's first token **past leading articles** (mirroring `TryPhrase`'s own `SkipArticles`-before-literals) and merges the bucket with the universal list by ascending rule index.
  > **The order proof:** every rule outside the merge provably cannot match — its entire first-token set excludes the sentence's key, and `TryPhrase` on it would pass any leading optionals freely and fail at its first non-optional item. Removing provable non-matches from an in-order walk cannot change which rule matches first, within or across buckets. **The message proof (the part the definition didn't demand but exactness does):** a skipped rule's one observable side effect is its near-miss `NoteFail` — always at the article-skipped position, with its first item's description, both precomputed at index build. Within one gap all skipped rules share that position and `NoteFail` keeps only the first arrival at any new furthest position, so **one simulated call per gap, carrying the gap's first rule's description, reproduces the linear scan's near-miss record word for word** — the G4 `fail:` pins and every teaching refusal stay byte-identical. Over-inclusion is fail-safe by the same token: an extra candidate is really tried, and its real `NoteFail`s are what the old loop produced.
  > **Rebuild-at-load, implemented as invalidate-on-mutation + lazy rebuild:** the flag drops on append, on override (in place — the count never moves, and an override can *reword the pattern's head*, so the flag is the only honest invalidation), on `EnglishResetGrammar`, and on G5's `RestoreRules` (append-then-pop and override-audition-then-restore both keep the count; a stale bucket entry could point past the restored store or drop a restored head's branch). A count sanity check backs the flag. During a vocabulary load the next dispatch is the next `test:` proof, so the index is rebuilt at load exactly as specified; a VBA state wipe zeroes the flag and rebuilds on first use.
  > **Ten pins added (`TestV7`):** order across buckets (an earlier slot-first rule beats a later bucketed one), order and G2 shape fall-through within a bucket, article-skipped keying on the hit path, bare-branch and stem/suffix surfaces reaching their buckets, the simulated near-miss matched word for word against the old wording (`A zzz.` → "I understood 'a' - then I expected 'set' but found 'zzz'"), in-place override rebuilding the index (`shove` → `nudge|shove`, the new head's branch must fire), a discarded try leaving a clean near-miss (never a subscript error), and the override-audition restore re-arming the original head's buckets (`twist|wiggle` narrowed to `wiggle` in audition; `Twist` must still fire after restore — the one case the count check is blind to).
  > **Expected deltas from the printed baselines:** self-test **+10** (357 → **367**); vocabulary **unchanged** (101 rules / 199 tests / 5 fails / 16 macros — no vocab file touched); **both goldens must diff EMPTY** — behavior-invariant is the claim, an empty diff is the witness, and per the standing rule a *non*-empty diff here is a finding in the loud direction; VerifyReport H1:H31 unchanged. **Performance prediction against the recorded before-number** (86 ms vocab / 94 ms translate / 303 rows): the translate dial should drop visibly (per-sentence rule tries fall from ~101 to bucket-size + universal, typically ≤5, plus an O(gaps) simulation of constant cost); the vocabulary dial flat-to-slightly-better (each `test:` proof now dispatches; rebuilds are cheap and amortized one-per-added-rule). The after-numbers come from the owner's Check line and the session-closing `VlaTimeIt`.
  > **Verification loop:** `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **367 / 0**) → reload vocabulary (expect **101/199/5/16**) → Run `english.txt` → `VerifyReport` (H1:H31 green) → `VlaWriteGoldens` + `git diff` (**predicted: both goldens diff empty**; treat any diff as a stop-and-report finding) → **standalone-topology check owed** (`VLA_English.bas` ships): dev workbook closed, fresh workbook, built add-in, Check + Run a program — dispatch state is module-level but armed-and-read inside one Check/Run call chain, no cross-boundary state, yet the check is owed because the module ships. No reload-list change (no new module); VLA_DevRig untouched.
- **✅ P.2 — V8: the string-building pass — VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(shipped as V8.0; compile clean, all tests passed dev + fresh workbook — the owed standalone check satisfied, the manifest cache's VLAr_Source sheet tier exercised. After-numbers, dev-workbook `VlaTimeIt`: vocab 76 (prev 69), translate 49 (prev 38), transpile **393 (prev 385)**, run 602 (prev 582) — every dial FLAT within `Timer`'s ~16 ms granularity plus run-to-run noise; the fresh-workbook Check line 129/70 likewise a cold different-venue reading, recorded verbatim. **The pass's own honesty clause, invoked as written in advance:** at 303 rows the gain is sub-perceptual — the anticipated outcome, recorded before the machine spoke, because the pass buys the scaling law for D3/F2-sized programs, where quadratic recopying would have dominated, not today's corpus. **CollGet verdict, now read as the record committed to:** the transpile dial did NOT move after the string quadratics were removed — strings were not the dominant transpile cost at this scale, so the exception-throw suspicion (`Substitute`/`FormLine` misses) GRADUATES from suspicion to lead. It stays unbought: the Dictionary swap is structural, and 393 ms is tolerable today — but any future pass at the transpile leg starts THERE, not at strings, and this sentence is where the dial said so.)* *(definition on Alpha 4).* Pre-sized buffer builder behind two helpers for `EnglishToVla` / `VlaTranspile` / the loaders; plus the checklist — manifest caching per resolve session, early-bound/cached ADODB, `CollGet` hot-path audit. Same three witnesses as V7. Measure per leg before and after; if V6's dial says a leg is already sub-perceptual, record the number and *skip the work* — the dial exists to prevent optimization as recreation.

  > **V8.0 record (shipped blind — the machine has not spoken).** Touched: `VLA.bas` (core L3.0 → **V8.0** — the core's first bump since L3), `VLA_English.bas` (→ V8.0), `VLA_Tests.bas` (→ V8.0). Before-numbers this pass answers to, from the pasted four-dial baseline: **transpile 388 ms, run 641 ms** (dev-workbook prevs 396/594 post-V7: 385/582 — the transpile leg's flatness under V7 is exactly the headroom this pass spends).
  > **The builder:** one pre-sized buffer grown by doubling and written with `Mid$` assignment, behind exactly the two tiny helpers the definition ordered (`SbAdd`, `SbText`) — Private in each module that needs them, a rule-12 twin pair (Privates are invisible cross-module, and a hot helper shouldn't pay a cross-module call anyway; the full rationale comment lives in VLA, the twin points at it). Converted, chosen by the rule *"does this loop's result grow with the program?"*: in the core — `VlaTranspile`'s top accumulator, `EmitBody`'s per-statement loop (a 5,000-sentence main lives or dies there), `EmitTop`'s `begin` unroll, `VlaExpandText`'s per-form loop, `WriteDatum`'s element join, `WritePretty`'s multi-line join; in English — `BuildSub` (main IS the program for a typical script), `JoinStmts`, `JoinSigs`, and `BuildStepInfra`, whose one-case-per-sentence `select` is the pass's textbook quadratic. Byte-invariant by construction: the builder emits the same bytes the `&` chain emitted, only the O(n²) recopying is gone. **Deliberately NOT converted, recorded so the restraint is auditable:** per-sentence token joins (bounded by sentence length — linear overall), the defmacro prelude (fixed-size constant), and `EnglishToVla`'s `outParts` (grows per SUB, a handful of appends, and its three PREPENDS don't fit a builder — O(subs)·copy, acceptable; revisit only if a dial ever says otherwise).
  > **The checklist, all three items:** (1) **Manifest cached per resolve session** — at the CALLER (`mManifestCache` in VLA_English), deliberately not in VLA_Runtime: the runtime is shipped payload and stays untouched at S5.2, no version bump, no payload byte drift. Cached only when non-empty, so the soft-failure mode (no VBE trust, missing sheet) retries per call exactly as before; a VBE edit wipes module state and the cache with it, a rebuilt add-in is a new session — one successful read is session-stable by construction. (2) **ADODB stream cached** (`mVocabStream`) — created once, `Open`/`LoadFromFile`/`Close` per read (`CreateObject` was the cost); the ANSI-fallback path drops the cached object entirely so a wedged or half-open stream cannot survive its own failure. Early binding rejected: it demands a project reference the add-in cannot assume. (3) **`CollGet` audit — verdict recorded, code stands:** the trap INSTALL is near-free, the THROW is the cost, paid only on a miss; the miss-dominant hot paths are `Substitute` (template symbols outside the bindings — most symbols of most templates) and `FormLine` (macro-generated forms carry no line). The throw-free alternative is a `Scripting.Dictionary` swap — structural, not bought blind; the transpile dial AFTER this pass's string conversion is the evidence that decides whether it's ever bought at all. Audit comment placed at the core `CollGet` definition.
  > **Ten pins added (`TestV8`)** — the builder is Private twice over, so every pin drives the public surface it serves: empty program transpiles to exactly the header (zero-length truncation, no padding leak); a 200-statement main crossing the doubling boundary repeatedly is deterministic byte-for-byte, emits all 200 statements once each, and leaks no padding past the final line break; flat and multi-line pretty output byte-exact (the width-90 split, the 4-space child indent); translated statements keep their order through the join; and the manifest cache is transparent — a typo'd helper still refuses BY NAME, a second check answers identically, a real runtime helper still passes. The stream cache carries no pin (`VocabReadFile` is Private; the owner's Reload is where any wedge would be loud) — recorded here instead.
  > **Expected deltas from the printed baselines:** self-test **+10** (367 → **377**); vocabulary **unchanged** (101/199/5/16); **both goldens must diff EMPTY** — same claim as V7, same witness, same loud-direction rule if a diff appears; VerifyReport H1:H31 unchanged. **Performance prediction, stated with V6's honesty clause in force:** at 303 rows the accumulators are mid-sized, so expect a REAL but MODEST transpile drop — the pass's full value only shows at D3/F2 program sizes (5,000 rows), where quadratic recopying would have dominated; if the dial says today's gain is sub-perceptual, that is the definition's anticipated outcome recorded in advance, not a failure — the pass buys the scaling law, and the CollGet verdict reads whatever the dial says next.
  > **Verification loop:** `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **377 / 0**) → reload vocabulary (expect **101/199/5/16**) → Run `english.txt` → `VerifyReport` (H1:H31 green) → `VlaWriteGoldens` + `git diff` (**predicted: both goldens diff empty**; any diff is a stop-and-report finding) → **standalone-topology check owed** (`VLA.bas` and `VLA_English.bas` both ship): fresh workbook, built add-in, Check + Run — note the manifest cache exercises the VLAr_Source sheet tier there, the path the dev workbook never takes. No reload-list change; VLA_Runtime, VLA_DevRig, VLA_IDE, VLA_Build untouched. After verification: the four dials again — transpile against **385/388**, run against **582/641** — and the CollGet verdict is read against whatever transpile then says.

## Bucket 2 — LISP *(bench items; instruments first, showpiece last; definitions on Alpha 4 Band L unless marked)*

- **✅ L.1 — L6: `VlaTryValue` + `*1/*2/*3` history — VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(shipped as L6.0 + the L6.1 pin fix; compile clean, self-test 394/0, all tests pass — the one first-run pin failure was adjudicated as pin design, split atomic, and is green; the in-expression-ByRef lesson stands recorded below)* — the true eval (an instrument; everything below iterates on it). *(Definition on Alpha 4: wrap the input in a `(function …)`; `Application.Run` returns the value; `*1/*2/*3` recall via the Names pattern; dev-rig only, rides L2's machinery.)*

  > **L6.0 record (shipped blind — the machine has not spoken).** Touched: `VLA_DevRig.bas` (V6.2 → **L6.0**), `VLA_Tests.bas` (→ L6.0). Engine, English, runtime, loader, IDE, build: all untouched — the item is dev-rig only, exactly as defined.
  > **The design as built.** `VlaTryValue "(+ 1 2)"` prints `=> 3`: the input splits through **one shared scanner** — `TrySplit`, extracted from `VlaTryBuild` so both builders ride the same balanced-chunk walk, string rules, comment rules, and imbalance refusals (a second scanner would be a second set of edge cases; the G10 one-expansion doctrine applied to scanners). Module-level definitions hoist exactly as `VlaTry` hoists; every body chunk but the LAST is a statement; the last chunk wraps as `(return …)` inside `(function vla-scratch-value () Variant …)` — the emitter's existing `return` form and the existing hyphen mangling do the rest, and `Application.Run` hands back the function's value. Inject/run/sweep is `VlaTry`'s own machinery unchanged: same `VLA_Scratch` module, same rename-aside `TryDropScratch`, same D1-threaded explicit-host injection.
  > **The history.** `*1/*2/*3` recall works in BOTH `VlaTry` and `VlaTryValue` (only `VlaTryValue` WRITES — a Sub run yields nothing to store). Recall is a textual substitution BEFORE the scratch is built — token-boundary aware (`*12` is not a slot), never inside `"…"` strings or `;` comments, same character rules as the scanner. Values persist in workbook names `VLAt_Star1/2/3` — the `VlaTimeIt` pattern, and for the same reason stated in the header: **every scratch injection wipes module-level state (S5.1), so module state cannot remember anything across evals**; Names survive the wipe. Storable values are scalars re-read as themselves: numbers through `Str$` (period decimal on every locale — `CStr` is not), strings re-quoted with the tokenizer's own `\"` / `\\` escapes (~180-char ceiling), True/False. Objects, arrays, Empty, Null, dates store as `#<…>` markers that **refuse substitution with words** — `*1` never silently injects garbage.
  > **Honest limits, recorded in the header:** the last form must be an EXPRESSION — a Sub-shaped statement there surfaces VBA's untrappable compile modal during `Application.Run` (S4's verdict, `VlaTry`'s recorded limit in a new coat); a refused `Names.Add` loses one history slot, never the eval; the injection wipes loaded grammar — reload the vocabulary before the next Check/Run.
  > **Sixteen pins added (`TestL6`)**, all on the pure halves made Public precisely so the suite can reach them (`VlaTryBuild`'s recorded precedent — the inject/run/delete half mutates the live project and only the Immediate-window smoke judges it): the value wrapper byte-shape and its runnable transpile, statements-then-value ordering, definition hoisting with macro expansion inside the returned value, the bare-atom value, the definitions-alone refusal with words, boundary substitution for all three slots, the `*12` non-slot, string/comment immunity, the empty-slot and marker refusals with their words, number/string/Boolean literal round-trips including escape fidelity, the unstorable marker, and a recalled value riding into the next build.
  > **On-machine finding and fix (L6.1, tests only).** First run: 392 pass, ONE fail — `l6: an unstorable value becomes a refusing marker`, with the detail showing the EXPECTED marker text. Diagnosis by elimination across the sibling pins: the failing pin was the only one to read `ok` AFTER an in-expression ByRef mutation (`… VlaTryValueLit(True, ok) = "True" And ok`); the numbers pin mutates in-expression but never reads after, the strings pin reads only prior-statement state — both passed. Verdict: **the trailing read did not observe the in-expression ByRef write; in-expression ByRef mutation order is not a contract, and no pin may depend on it** — the pin design was the bug, not the rig function, whose both branches the passing pins already vouch for. Fix shipped as `VLA_Tests.bas` → **L6.1**: the compound split into two atomic pins, one side-effecting call per STATEMENT, `ok` snapshotted and printed in every detail — so if anything deeper is wrong the next run names it with full state instead of hiding it. Lesson carried for every future pin. `VLA_DevRig.bas` stays at L6.0 untouched.
  > **Expected deltas from the printed baselines (revised for L6.1):** self-test **+17** (377 → **394**); vocabulary **unchanged** (101/199/5/16); **both goldens diff EMPTY** (no engine or grammar surface touched — the loudest kind of empty prediction); VerifyReport unchanged. No dial predictions: L6 is an instrument, not an optimization.
  > **Verification loop:** **manual VLA_DevRig replace in the VBE FIRST** (the rig cannot reload itself — V6's precedent), then `VlaDevReload` (picks up VLA_Tests) → Debug → Compile → `VlaSelfTest` (expect **394 / 0**) → reload vocabulary → Run `english.txt` → `VerifyReport` → `VlaWriteGoldens` + `git diff` (predicted empty, both) → then the Immediate-window smoke that judges the impure half: `VlaTryValue "(+ 1 2)"` (expect `=> 3`), `VlaTryValue "(* *1 2)"` (expect `=> 6`), `VlaTryValue "(+ *1 *2)"` (expect `=> 9`), `VlaTry "(debug-print *1)"` (expect `9` — reads history, writes none), and one teaching refusal: `VlaTryValue "(f *3)"` after fewer than three evals would have said so with words; with three now stored, `VlaTryValue "*3"` (expect `=> 3` — the oldest survives). Reload the vocabulary after. **No standalone check owed** (dev-rig never ships — V6's precedent); no reload-list change (no new module).
- **✅ L.2 — L16: the small-verbs prelude batch — VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(shipped as L16.0; compile clean, self-test 400/0, goldens empty, standalone check satisfied. **Baseline correction adopted during verification:** the loader's return — authoritative, one increment per rule line, zero `override:` lines in the file — prints **102**, so the recorded composite "101 rules" was an assembled-record off-by-one with no printing source (the U.10 finding, bearing fruit); **the recorded baseline is now 102 rules / 199 tests / 5 expected fails / 16 macros**, and U.10's counters line is what makes any future drift visible at the moment it happens rather than at the next hand-count.)* (`swap!`, `dec!`, `returning`) *(proposal adopted into the bucket; definition on Alpha 4)* — cheap, golden through `VlaExpand`.

  > **L16.0 record (shipped blind — the machine has not spoken).** Touched: `VLA.bas` (→ **L16.0** — prelude text only), `VLA_Tests.bas` (→ L16.0). **Honest scope finding:** `dec!` — the definition's third verb — was discovered ALREADY on the bench: L3 shipped it beside `inc!`/`add!` and its pin has lived in the suite since. L16 therefore delivers **`swap!` and `returning`**, and the record says so rather than re-shipping a verb.
  > **The two templates, C1 doctrine throughout** (caller names the temporary, the template dims it — `let-one`'s precedent — so two uses sharing a name in one procedure collide at VBA's compile step, the loud failure the doctrine promises; values only, like `let-one`): `(swap! a b tmp)` → `(begin (dim tmp) (set! tmp a) (set! a b) (set! b tmp))`; `(returning tmp expr & body)` → `(begin (dim tmp) (set! tmp expr) body (return tmp))` — CL's `prog1` in statement clothes: compute the answer FIRST, tidy up, return the saved value. The tail rides `(return …)`, so `returning` inside a Sub meets `EmitReturn`'s **existing trappable teaching refusal** ("(return <value>) is only valid inside a function") — loud and worded at transpile time, never a compile modal; the design leans on that door and a pin holds it. Four-question audit: both are fixed-position substitution (`& body` splices whole into one slot), no iteration, no surgery, no expansion-time decisions.
  > **Six pins (`TestL16`):** `swap!` expansion byte-exact through `VlaExpandText` (the definition's goldens-through-VlaExpand rule) and its three moves emitted in order full-stack; `returning` expansion byte-exact with a two-form body proving the splice; the function-context full stack in order (`Dim tmp` → `tmp = (1 + 2)` → body → `f = tmp` → `Exit Function`); the Sub-context refusal BY ITS WORDS; and `swap!` expanding inside another template's spliced body (fixpoint through nesting).
  > **Expected deltas from the printed baselines:** self-test **+6** (394 → **400**); vocabulary counts predicted **unchanged** — CORRECTED during verification: no counters line is printed by ANY current code (the IDE Reload prints lint + Check timing only; the loader RETURNS rules-added, discarded by the IDE call), so the originally-flagged "report if other than 16 macros" check was not observable as written — the assistant's error, amended here. The observable form today: `?EnglishLoadVocabulary(path)` after a reset prints rules-added (expect **101**); the macro counter itself has no print or public query until U.10's counters line, where the check now lives. No L16 change can touch vocab-carried counts by construction (prelude and vocab macros live in different registries — the U.9 diagnosis's own finding); **both goldens diff EMPTY** (prelude macros are prepended at tokenize, never emitted; no corpus sentence uses the new verbs); VerifyReport unchanged.
  > **Verification loop:** `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **400 / 0**) → reload vocabulary (expect 101/199/5/16, see the counter note) → Run `english.txt` → `VerifyReport` → `VlaWriteGoldens` + `git diff` (predicted empty, both) → **standalone-topology check owed** (`VLA.bas` ships): fresh workbook, built add-in, Check + Run. Optional one-line L6-powered smoke of the new verbs: `VlaTry "(dim a) (dim b) (set! a 1) (set! b 2) (swap! a b t9) (debug-print a) (debug-print b)"` (expect `2` then `1`), and `VlaTryValue "(zz)"`-style probes remain at your disposal — the instrument proving itself on the first pass after its own. No reload-list change.
- **✅ L.3 — L7: `(time …)` / `(trace-form …)` — VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(shipped as L7.0; compile clean, self-test 407/0 dev + fresh, loader 102, goldens empty, standalone satisfied; both maiden smokes passed as expected — the first VLA-level micro-benchmark printed its labeled seconds through the L6 instrument, and trace-form bracketed its demo — with the smoke-line correction recorded in place: For emission does not dim its loop variable, so probe snippets dim their own, the anti-gensym rule teaching its own smoke test.)* — V6's spirit at VLA scale; L3's littermates. *(Definition on Alpha 4: prelude templates on the C1 temporaries-as-parameters doctrine; goldens through `VlaExpand`; pays V6's micro-benchmarks a VLA-level spelling.)*

  > **L7.0 record (shipped blind — the machine has not spoken).** Touched: `VLA.bas` (→ **L7.0** — prelude text only), `VLA_Tests.bas` (→ L7.0). Two templates, exactly the definition's shape: **`(time label tmp & body)`** → `(begin (dim tmp) (set! tmp (timer)) body (debug-print (& label ": " (- (timer) tmp) " s")))` — the start-stamp is a caller-named temporary, the definition's C1 clause licensing the parameter over its shorthand signature, `let-one`'s dim-inside precedent, collisions caught loudly by VBA's duplicate-Dim; **`(trace-form label & body)`** → entry/exit bracket prints (`>> label` … body … `<< label`) — zero temporaries, the label IS the trace, and NO expansion-time stringification anywhere (the caller supplies the label, so the four-question test passes without a whisper: fixed positions, whole-splice rest, no decisions, no iteration, no surgery). Emission facts verified before shipping: `(timer)` emits as `timer()` (the nullary-call path), n-ary `(& …)` rides the existing concatenation emission. **Honest limits stated in the prelude comment:** `Timer` granularity ~16 ms and midnight wrap (a run across it prints negative) — single-shot times are for the macro scale, and micro-benchmarks COMPOSE their reps: `(time "1000 adds" t1 (dotimes i 1000 (add! x 1)))` — the dotimes is the rep loop, no rep machinery in the template, V6's granularity note answered by composition.
  > **Seven pins (`TestL7`)** — CheckFrags-plus-order rather than byte-exact, reason recorded: both expansions exceed the pretty-printer's one-line width, and frags with InStr order pins hold the contract without pinning indentation. Covered: time's stamp/body/labeled-print frags and the stamps-BEFORE-reads-AFTER order; the full VBA scaffolding (`Dim t1`, `t1 = timer()`, body, `(timer() - t1)`); trace-form's brackets and their order; both bracket prints in emitted VBA; the littermates composing (trace inside time). The printed seconds are unpinnable for V6's recorded reason — Timer output is not a constant — so the smoke holds the dial.
  > **Expected deltas from the printed baselines:** self-test **+7** (400 → **407**); vocabulary loader return **unchanged at 102** (prelude-only change); **both goldens diff EMPTY**; VerifyReport unchanged.
  > **Verification loop:** `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **407 / 0**) → `EnglishResetGrammar` + `?EnglishLoadVocabulary(…)` (expect **102**) → Run `english.txt` → `VerifyReport` → `VlaWriteGoldens` + `git diff` (predicted empty, both) → **standalone-topology check owed** (core ships) → the L6-powered maiden smoke, the instrument chain one line each: `VlaTry "(dim x) (dim i) (time ""a million adds"" t1 (dotimes i 1000000 (add! x 1)))"` (expect a labeled seconds line — the first VLA-level micro-benchmark ever printed; CORRECTED during verification: the original smoke omitted `(dim i)` — For emission deliberately does not dim its variable, the anti-gensym rule holding even in loops, so the undeclared loop var would have met the compile modal under the scratch's Option Explicit — and the loop grew to a million so the maiden number clears Timer's ~16 ms tick instead of printing 0 s) and `VlaTry "(trace-form ""demo"" (debug-print 42))"` (expect `>> demo`, `42`, `<< demo`). Reload the vocabulary after. No reload-list change.
- **✅ L.4 — L17: defmacro docstrings — VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *("L17's successful compile/tests/build" — the owner's phrasing, taken as the word)* *(proposal adopted into the bucket)* — the smallest engine touch; feeds L.5.

  > **L17.0 record (shipped blind — the machine has not spoken).** Touched: `VLA.bas` (→ **L17.0**), `VLA_Tests.bas` (→ L17.0). The touch, as small as promised: `DefineMacro` accepts an optional string literal between the signature and the template — **doc only when at least one more form follows; a lone string IS the template** (CL's own disambiguation, so string-valued macros stay expressible; the rule is pinned from both sides). One docstring at most; a second string belongs to the template and the engine judges it as it always has. The doc is stored as the record's fourth item — positionally backward-safe, every existing reader indexes 1–3 — and **never enters the template, so expansion ignores it by construction** (the invariance half). The seam: **`VlaMacroDoc(name)`** → the doc or "" for undocumented/unknown, case-insensitive like the table, with the LIFETIME stated plainly at the definition: the macro table rebuilds on every parse, so the honest query flow is parse-then-ask — exactly what L11's `(doc …)` will ride, and vocabulary-carried macros answer here after any load or translation because their probe rides the same table. Escapes stay as written; L11 can prettify.
  > **Seven pins (`TestL17`):** a documented macro expands byte-identically to an undocumented one; the doc reads back; lookup case-insensitivity; the lone-string rule from both sides (expansion carries the string, doc reads ""); doc-plus-multi-form templates begin-wrap the forms AFTER the doc; undocumented and unknown names both read ""; and a **vocabulary-carried** docstring surviving the macro probe and reading back (0 rules added, doc intact), grammar reset after.
  > **Expected deltas from the printed baselines:** self-test **+7** (407 → **414**); loader return **unchanged at 102**; **both goldens diff EMPTY** (no corpus or vocab text carries a docstring, and expansion is invariant by construction); VerifyReport unchanged.
  > **✅ L17-D — the vocabulary documentation pass — VERIFIED ("L17 works as expected" — the owner's word; the .vla golden moved by exactly the docstrings, the .vba stayed still, and the committed goldens are the NEW baseline every later empty-diff prediction measures against): `spreadsheet.vocab` rewritten with all 16 carried macros documented** — one-line docstrings inserted IN PLACE on each `macro:` line (the multi-line `vla-stamp-cell` docs on its signature line), **file line count byte-preserved** so every test citation and teaching message stays stable; no rules, tests, or templates touched; quote-balance and escape-free content verified (apostrophes only). This is L11's regression substrate: after any load, `?VlaMacroDoc("set-formula")` answers `write a formula string into a range`. **Expected deltas — note the golden prediction INVERTS for the first time this ledger:** loader return **unchanged at 102**, 16 macros, 199/5 proofs re-run green (docs are expansion-invariant, L17's pin); self-test **unchanged at 414** (no module touched, no version bumps, no standalone check — data file); **the `.vla` golden diff must be NON-EMPTY and must be EXACTLY the 16 docstrings appearing inside the appended defmacro tail** — the L4 comment block's own recorded contract ("changes the .vla golden by exactly the appended defmacro") exercised at scale, an intentional reviewed contract change; **the `.vba` golden must diff EMPTY** — docstrings never emit, and any `.vba` movement is a stop-and-report finding. **Verification:** `EnglishResetGrammar` + `?EnglishLoadVocabulary(…)` (102) → Run `english.txt` → `VerifyReport` green → `VlaWriteGoldens` + `git diff` (**one non-empty diff, one empty, exactly as stated — review the .vla diff and confirm it is docstrings only**) → `?VlaMacroDoc("set-formula")` smoke.
  > **Verification loop:** `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **414 / 0**) → `EnglishResetGrammar` + `?EnglishLoadVocabulary(…)` (expect **102**) → Run `english.txt` → `VerifyReport` → `VlaWriteGoldens` + `git diff` (predicted empty, both) → **standalone-topology check owed** (core ships) → optional one-line smoke through the instrument chain: `VlaTryValue "(zz)"`-free this time — instead, in the Immediate window after any Check: `?VlaMacroDoc("set-formula")` (expect "" today — the vocab's macro is undocumented, which is precisely the vacuum L11 and a one-line vocab edit will fill). Reload the vocabulary after any VlaTry use. No reload-list change.
- **✅ L.5 — L11: `VlaApropos` + `(doc …)` — VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(the full L11.0 → L11.3 cycle: 431/0 dev + fresh, goldens empty, all four smokes passing with the corrected ordering — the bench self-documenting, the helpers one per line, the doc form speaking the documented prelude, and the parse-scope teaching line demonstrating the recorded limit on cue. The cycle's on-machine findings each hardened the tool: state-independence via the carry, stale-premise pins made self-sufficient, the manifest's space-joined format read correctly, and the wipe taught in words.)* — discoverability over the manifest + macro table + docstrings. *(Definition on Alpha 4: name-match over `VlaHelperManifest` + the macro table + docstrings; pays discoverability for Tier-2's growing runtime and Explain's `(doc …)` panel.)*

  > **L11.0 record (shipped blind — the machine has not spoken).** Touched: `VLA.bas` (→ **L11.0**), `VLA_Tests.bas` (→ L11.0). Three pieces. **The name slot:** macro records gain item 5, the name as written — VBA Collections cannot enumerate their keys, so apropos reads the name from inside the record; positionally backward-safe like L17's item 4. **`(doc <name>)` — an emitter statement form** (the emitter's privilege, like `at-line`): the doc is looked up in THIS parse's table at transpile time and baked into a `Debug.Print`, so the running program simply speaks it; three wordings, all pinned by emission — the doc line for a documented macro, `(no documentation)` for a bare one, and for an unknown name a teaching message stating the parse-scope truth plainly (*vocabulary macros ride loads and translations, not raw scratches*) instead of pretending. **`VlaApropos(pattern)` / `VlaAproposText(pattern)`** — the pure half returns the report and is what the suite pins (the `VlaTimeIt` precedent for print-shaped tools); matches case-insensitively over macro NAMES and DOCSTRINGS, then over the **runtime helper manifest** per the definition (helpers print as `name - (runtime helper)`); definition order, macros first; empty pattern lists the whole bench; a miss answers with words, not silence. Manifest read fresh per call (a human asks once — no cache) and SOFT on failure, the resolve check's own doctrine. Lifetime as L17 recorded: parse-then-ask — and a vocabulary load ENDS with every carried macro parsed, so **load-then-ask sees the whole bench**, the natural dev flow and a pinned fact.
  > **Nine pins (`TestL11`):** the three (doc) emissions by their words; apropos name-hit and docstring-hit; the empty-pattern bench listing (prelude's `when` and `swap!` both present); the manifest leg (`vlashowerror - (runtime helper)`, leaning on the dev workbook's live manifest — the same environmental fact the resolve-check pins have leaned on since S4); the worded miss; and load-then-ask seeing a vocabulary-carried macro.
  > **Expected deltas from the printed baselines:** self-test **+9** (414 → **423**); loader return **unchanged at 102**; **both goldens diff EMPTY against the L17-D committed baseline** (no corpus sentence speaks `doc`, apropos is never emitted); VerifyReport unchanged.
  > **Verification loop:** `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **423 / 0**) → `EnglishResetGrammar` + `?EnglishLoadVocabulary(…)` (expect **102**) → Run `english.txt` → `VerifyReport` → `VlaWriteGoldens` + `git diff` (predicted empty, both) → **standalone-topology check owed** (core ships) → the maiden smokes, each one line: `VlaApropos "formula"` right after the load (expect `set-formula - write a formula string into a range` — the L17-D substrate answering its first question); `VlaApropos` bare (the whole bench: 16 documented vocab macros, the prelude, the runtime helpers); `VlaTry "(doc when)"` (expect `when: (no documentation)`); and — deliberately — `VlaTry "(doc set-formula)"` (expect the parse-scope teaching line — the recorded limit demonstrating itself). Reload the vocabulary after the VlaTry lines. No reload-list change.
  > **L11.1 — the fix-pass the maiden smokes ordered (SHIPPED aboard the L11 verification cycle — the machine has not spoken on it).** Touched: `VLA.bas` (→ L11.1), `VLA_English.bas` (V8.0 → **L11.1** — the first English touch since V8, which triggers the recorded promise: **U.9 and U.10 ride aboard**), `VLA_IDE.bas` (S5.6 → L11.1; note the brief's S5.5 stamp was stale), `VLA_Tests.bas` (→ L11.1). **The finding:** smoke 1 missed — apropos answered "(nothing matches)" for a loaded vocabulary — because parse-then-ask made the answer depend on which machinery parsed LAST: technically true, pinned, and wrong for a human-facing tool. **The fix, at the correct layer:** English pushes the carried defmacro text down at every load (`VlaAproposCarry`, dependency direction preserved — English already depends on core) and clears it at reset; apropos **re-parses prelude + carry on every ask**, making its answer state-independent; `(doc …)` stays parse-scoped ON PURPOSE (an emitter form; its teaching message says so). One suite consequence handled honestly: L11.0's name-hit/doc-hit pins defined their macro via a transient parse — exactly what the fix now ignores — so they were rewritten to arrive by LOAD, the way a user's macro would. **Also aboard:** the **prelude now documents itself** — all 18 prelude macros carry docstrings (the bare-apropos smoke showed a wall of "(no documentation)"; it now reads as a bench), expansion-invariant, goldens untouched. **U.9 shipped:** same-path re-carry teaches *reset first (EnglishResetGrammar) or use the IDE's Reload* — the cross-file collision keeps the pinned rename wording verbatim, both branches pinned. **U.10 shipped:** `EnglishVocabStats()` (counts cumulative since reset, source names the last load) printed by the IDE reload — `loaded: 102 rules, 16 macros, 199 tests (5 expected fails) from spreadsheet.vocab` — and the long-deferred macro-counter check finally becomes observable on its first print. **Six pins (`TestL11_1`):** the carry surviving an unrelated parse (THE fix pin), reset restoring prelude-only, the self-documenting prelude, both U.9 wordings by their words, and the counters line as one exact string. **Expected deltas:** self-test **+6** (423 → **429**); loader **102**; on the IDE Reload the NEW third line: the U.10 counters (predict **102 / 16 / 199 / 5** — the recorded baseline finally printed by the machine that owns it); **both goldens diff EMPTY** (prelude docs never emit; carry and stats touch no translation); VerifyReport unchanged; **standalone check owed** (core, English, and IDE all ship). **L11.2 — the three-fail fix (tests + one core line; SHIPPED into the same cycle).** First L11.1 run: 3 fails, all pin-side, diagnosed from the printout alone. Two were STALE-PREMISE pins — they borrowed `when` as their undocumented specimen, and L11.1's prelude docs invalidated the premise (one failure's detail was literally the doc form working perfectly on the newly documented prelude); both now define their own bare macro, and the lesson joins L6.1's on the ledger: **a pin's premise is not a contract — when a pass changes the premise, the pass updates the pin, and borrowed specimens are how that gets missed.** The third caught a real format assumption: `VlaHelperManifest` is SPACE-joined on one line — visible in the owner's own smoke paste, missed by the assistant — so the split assumed line breaks and produced one giant name; core now normalizes all whitespace and splits on spaces, which also renders the helper list one-per-line, simply the better report. Expected: self-test **429 / 0** (counts unchanged — fixes in place); the `VlaTry "(doc when)"` smoke now prints the DOC (`when: run the body when the test is true`) — a better demo than the vacancy it replaced, recorded as the smoke's new expectation. **L11.3 — the wipe finding (SHIPPED into the same cycle; core + tests).** Smoke 1 missed AGAIN after L11.1's carry fix, and the mechanism is the ledger's own oldest fact: **S5.1 — every injection (a Run, any VlaTry) wipes module-level state**, and the carry was module state — the exact mistake L6's header warned against and solved with workbook Names, violated by the assistant's own fix. The honest truth the smoke surfaced: after a wipe, NOTHING in memory knows the vocabulary until the next load — by design, consistently, on both sides of the seam (English's registries die in the same wipe, which is why "reload before the next Check" has been the standing note since S5.1) — so apropos answering prelude-only there is CORRECT, and the fix is honesty, not persistence: the miss line now distinguishes *no match* from *nothing loaded* ("no vocabulary is carried right now - a Run or scratch injection wipes module memory; Reload, then ask again"), two pins hold both wordings (429 → **431**), and the smoke instruction is corrected: **ask apropos between a Reload and any Run or scratch.** A Names-backed carry was considered and rejected on the record: vocab macro text exceeds the proven Name-storage ceiling, and persisting one tier of a wiped world would make apropos LIE about the rest of it. **Fresh-workbook calling convention, answered:** `VlaApropos` DOES ship in the add-in — the Immediate window just resolves unqualified names against the active project only, so from a user workbook call `Application.Run "'<add-in name>.xlam'!VlaApropos", "formula"` — and the real user-facing answer is an IDE surface, adopted below as U.12.
- **✅ L.6 — L8: the check family — VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(the full L8.0 → L8.2 cycle: 442/0 dev + fresh, goldens empty, all three smokes — the silent pass, the first deliberate failure speaking its label-expected-got line in the Immediate window with the dialog retired, and the operator refusal in words. The cycle's findings each became permanent teaching: the emission-verified-pin lesson and the scratch self-handler.)* — tests written in VLA, runnable via `VlaTry`, embeddable as raw rows. *(Definition on Alpha 4: `(check= label expected expr)` etc. as templates expanding to if-not-raise; pays Tier-2 spike self-checks and user-side assertions someday.)*

  > **L8.0 record (shipped blind — the machine has not spoken).** Touched: `VLA.bas` (→ **L8.0**), `VLA_Tests.bas` (→ L8.0). **The family, "etc." resolved to the two workhorses** (more verbs are one-line adds when demanded — the small-verbs doctrine, recorded): `(check label cond)` → if-not-raise with the label; `(check= label expected expr)` → inequality-raise carrying label, expected, and actual (*"check failed: math - expected 4 but got 5"*). Templates on the L7 pattern — the caller supplies the label, so no expansion-time stringification exists anywhere near the four questions; both documented on the bench. **Honest limit stated in the prelude:** check='s operands appear in the comparison AND the failure message, so they re-evaluate when the check FAILS — bind side-effecting expressions with `let-one` first; a passing check evaluates each operand once. The raise rides the existing `(err.raise …)` form — trappable, so a failing check surfaces through `VlaTry` as a worded refusal, which is the whole point.
  > **The rider — the owner's `VlaTry "(+ 1 2)"` finding, ADJUDICATED: not a bug in what was emitted (an operator form genuinely is not a statement) but a REAL finding in how it failed** — `Call +(1, 2)` met VBA's untrappable modal when the emitter could refuse at transpile time. The guard now lives at `EmitCallStmt`: an operator head in statement position raises with words and directions (*"'(+ ...)' is an expression, not a statement - use its value ((set! x (+ ...))) or evaluate it with VlaTryValue"*) — trappable, so `VlaTry` catches and teaches; ordinary call statements untouched. Nothing legitimate ever reached the old path (it always emitted broken VBA), so the guard is pure teaching gain.
  > **Seven pins (`TestL8`):** both expansions by frags, the expected-then-got message order, the full-stack guarded raise, the family documented on the bench, the operator refusal by its words WITH its directions, and ordinary `Call` emission unchanged. A failing check's actual RUN is the smoke's job — raising in-suite would require injection, V6's recorded reason.
  > **L8.2 — the dialog finding (rig + tests; SHIPPED into the same cycle).** Smoke 2 raised its perfect message THROUGH VBA'S RUNTIME DIALOG: **runtime errors do not cross `Application.Run` into the caller's handler** — the same seam that gave translated programs their `vla-fail` instrumentation and V6.1 its step-38 dialog — so `VlaTry`'s worded catch was never reachable, and the recorded smoke expectation ("could not run the scratch: …") was wrong on the rig's own recorded physics. The fix is the one translated programs already use: **the scratch catches its own errors** — both builders now arm `(on-error goto vla-try-oops)` before the body; the sub scratch exits before the label and speaks `  (scratch error: <description>)` to the Immediate window; the value scratch prints the same and falls out returning Empty, so history stores the `#<empty>` marker that already refuses recall with words. Fixed label name, rig-owned; a colliding user label meets VBA's duplicate-label step (C1's failure). An EMPTY scratch stays bare — nothing can error in it, pinned. **Three L6-era frags carried the old closer placement as a premise** (the stacked `)` now lands on the handler's last line) and were updated — the L11.2 lesson applied the same day it was needed. Four new pins (`TestL82`): handler-body-exit-label order, the bare empty scratch, the value scratch's arm-return-speak order, and the wrapped scratch transpiling runnable. **VLA_DevRig → L8.2 — the manual-replace-first step applies again** (the rig cannot reload itself). Expected: self-test **442 / 0** (+4); smoke 2 RE-RUN now expects the words in the Immediate window — `  (scratch error: check failed: math - expected 4 but got 5)` then `(ran and cleaned up)` — **the dialog retired**; every other prediction stands.
  > **L8.1 — one-fail fix (tests only).** First run: 437 pass, one fail — the full-stack pin expected statement-style `Err.Raise 5`, but `(err.raise …)` rides the GENERIC CALL path, emitting `Call err.raise(5, "check", …)` — legal VBA that raises identically, and exactly how `with-fast-excel` has emitted all along (the dev compile passing was itself the evidence). The code is correct; the pin asserted an ASSUMED emission. Fix: the frag now matches the verified emission; changing the emitter instead was considered and rejected (cosmetic, and a golden-drift risk wherever existing expansions ride the call form). **Lesson recorded beside L6.1's and L11.2's: pin frags come from verified emission, not from VBA idiom** — the L7 discipline (`(timer)` was checked before pinning) applied unevenly, now applied always. Expected on re-run: **438 / 0**, counts unchanged.
  > **Expected deltas from the printed baselines:** self-test **+7** (431 → **438**); loader **102**; counters line 102/16/199/5; **both goldens diff EMPTY**; VerifyReport unchanged.
  > **Verification loop:** `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **438 / 0**) → Reload (counters line) → Run `english.txt` → `VerifyReport` → `VlaWriteGoldens` + `git diff` (predicted empty, both) → **standalone-topology check owed** (core ships) → the maiden smokes, one line each: `VlaTry "(check= ""math"" 4 (+ 2 2))"` (expect silence — *(ran and cleaned up)*, a passing test's whole ceremony); `VlaTry "(check= ""math"" 4 (+ 2 3))"` (expect *could not run the scratch: check failed: math - expected 4 but got 5* — the first VLA unit test ever to fail, on purpose); and the rider's own demo, `VlaTry "(+ 1 2)"` re-run (expect the worded refusal naming VlaTryValue — the modal from the screenshot, retired). Reload the vocabulary after. No reload-list change.
- **✅ L.7 — L12: `VlaFormat` + balance hints — VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(450/0 dev + fresh, goldens empty, smokes clean — the canonical formatter that refuses to expand, the count in words both directions, and the L0 capture hint pinned at last.)* *(definition on Alpha 4: the L1 writer as canonical formatter for raw rows; the L0 scanner's balance count surfaced as a Check hint; pays raw-row ergonomics and codifies S1.2's house style)*.

  > **L12.0 record (shipped blind — the machine has not spoken).** Touched: `VLA.bas` (→ **L12.0**), `VLA_English.bas` (→ L12.0), `VLA_Tests.bas` (→ L12.0). **Honest scoping finding first:** the L0 capture ALREADY counts — an unbalanced raw row has refused with *"N '(' still open"* since the raw-row pass shipped, unpinned; the definition's balance-hint half was substantially delivered back then, and this pass records that, pins it at last, and completes the family. **What shipped:** `VlaFormat(source)` — the L1 writer as THE canonical formatter: tokenize, parse, pretty-print; **no prelude and NO EXPANSION** (what you wrote is what formats — `(when …)` stays `(when …)`, pinned); parse errors propagate with C2's hinted messages, and the function zeroes the transpiler's line offset so *"opened at vla line N"* numbers the text being formatted, not a prior parse's prelude (a module-state hazard caught in the blind sweep). `VlaBalanceHint(text)` — the count as words, pure and pinned, BOTH directions (*"opens 2 forms it never closes"* / *"closes 1 form it never opened"*), string-aware with the tokenizer's OWN escape rules (a naive quote-toggle miscounts `\"` — the vocab scanner's simpler toggle is noted as a recorded observation, not touched blind) and ;-comment-aware. English's `ValidateRawVla` asks the hint BEFORE the probe, so any unbalanced form arriving through other doors gets the friendly arithmetic instead of the parser's structural message.
  > **Eight pins (`TestL12`):** flat normalization, one-top-form-per-line, the width-90 split byte-exact against the L1 writer's own shape, the NEVER-expands pin, the hinted refusal riding through `VlaFormat`, both count directions plus balanced silence, escape-and-comment fidelity, and the L0 capture's own count — shipped then, pinned now.
  > **Expected deltas from the printed baselines:** self-test **+8** (442 → **450**); loader **102**; counters line 102/16/199/5; **both goldens diff EMPTY** (formatting is a new surface, nothing existing re-formats); VerifyReport unchanged.
  > **Verification loop:** `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **450 / 0**) → Reload (counters line) → Run `english.txt` → `VerifyReport` → `VlaWriteGoldens` + `git diff` (predicted empty, both) → **standalone-topology check owed** (core and English ship) → maiden smokes, one line each: `?VlaFormat("(let-one x (+ 1 2) (debug-print   x))")` (expect the canonical spacing back); `?VlaFormat("(when (= 1 1) (debug-print 1))")` (expect `when` STILL `when` — the formatter that refuses to expand); `?VlaBalanceHint("(a (b")` (expect the definition's own sentence); and `VlaTry "(zz (yy 1)"` — wait, that is a capture-level refusal: run it as a program row instead if desired, or trust the pin. Reload the vocabulary after any VlaTry. No reload-list change.
- **✅ L.8 — L15: the macro-stepper — VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(457/0, goldens empty, both smokes — the first macro expansion ever shown one application at a time, narrated frames from as-written to fixpoint.)* — `VlaExpand` step-N on the existing odometer. *(Definition on Alpha 4: "show application 3 of 7", reading `mExpandFired`; pays L4-era dialect-macro debugging and teaching material for VLA_GUIDE.)*

  > **L15.0 record (shipped blind — the machine has not spoken).** Touched: `VLA.bas` (→ **L15.0**), `VLA_Tests.bas` (→ L15.0). **The mechanism: a BUDGET on the odometer** — both application sites gain one guard line (`mBudgetOn And mExpandFired >= mExpandBudget` → the application simply does not fire; the walk continues, children stay walked, the form stays itself). The gate is a Boolean-plus-Long pair BECAUSE a bare Long's default 0 would silently budget every parse to zero — the hazard named in the declaration comment. **`VlaExpandStepText(source, stepN, ByRef totalApps)`** — pure and pinned: expands once to LEARN the total, then re-expands budgeted; step 0 is the parse as written, a step at or past the total is the fixpoint, and the budget restores on every exit including the error path (a stepper must never leave the engine stingy — a residue pin holds it). **The load-bearing claim, stated and pinned: determinism** — the expansion walk is a fixed order, so the first N applications of any two runs are the SAME N; a budget prefix is THE prefix, which is what makes "application 3 of 7" a fact rather than a sample. **`VlaExpandStep(source[, stepN])`** — the narrator: no argument walks every frame from "as written" to the fixpoint (the printout that IS the macro chapter of VLA_GUIDE); a stepN prints one frame.
  > **Seven pins (`TestL15`)** on a two-application source whose intermediate is fully predictable (`when` around `inc!`): the odometer total; step 0 untouched; **step 1 applying the outer macro and ONLY the outer** (the frame that shows expansion is outside-in); step 2 byte-equal to the full expansion; past-the-total clamping with the total still reported; the budget spending in walk order across sibling top forms; and the no-residue pin.
  > **Expected deltas from the printed baselines:** self-test **+7** (450 → **457**); loader **102**; counters line 102/16/199/5; **both goldens diff EMPTY** (the budget gate is dormant — `mBudgetOn` defaults False — and no production path steps); VerifyReport unchanged.
  > **Verification loop:** `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **457 / 0**) → Reload (counters line) → Run `english.txt` → `VerifyReport` → `VlaWriteGoldens` + `git diff` (predicted empty, both) → **standalone-topology check owed** (core ships) → the maiden smokes: `VlaExpandStep "(when (= 1 1) (inc! x))"` (expect three frames narrated — as written, the `if` with `inc!` still folded, the fixpoint — the first macro expansion ever SHOWN one application at a time) and `VlaExpandStep "(swap! a b t1)", 1` (one frame of the three-move swap). No injection — no vocabulary reload needed after. No reload-list change.
### Gathered at session close — Performance gap proposals awaiting the owner's adoption call *(the VBA master's pothole survey; each names its mechanism, why today's dial hides it, and the linear-or-better collapse. Standing rule applies: nothing here opens without a before-number — and P-PROF exists to supply them)*

- **P-PROF — the transpile sub-dial (the microscope, FIRST).** The 390 ms transpile leg is one opaque number; split it into phase dials — tokenize / parse / expand / emit — the way V6 split the loop. Every item below stops being a suspicion and becomes a reading; the recorded CollGet lead gets confirmed or retired by numbers. An afternoon, dev-rig class.
- **P-NTH — positional Collection indexing is O(i), and shipped loops do it.** VBA Collections are linked lists; `.Item(i)` walks from the head, so `For i … Nth(lst, i)` loops are QUADRATIC in list length — `EmitBody`, `WritePretty`, and the begin-unroll all qualify. **V8 fixed the string half of EmitBody and left the indexing half quadratic:** ~45K item-hops at 300 statements (invisible), ~12.5M at a 5,000-sentence main (seconds). Collapse: `For Each` with a skip counter, or copy-to-array once. Behavior-invariant; the likeliest owner of the missing transpile mass.
- **P-TOK — the same trap on the TRANSLATION side, masked by today's dial.** If `TokAt` indexes a Collection positionally, document parsing is O(tokens²) — flat-looking at 300 rows, a cliff at 5,000, eating V7's dispatch victory from below. Collapse: copy the token stream to a String ARRAY once after tokenize — O(1) indexing forever. The purest unnoticed pothole on the list: the healthy 49 ms actively hides it.
- **P-DICT — exception-free lookups (the recorded CollGet lead, executed).** The throw on a miss is the cost; `Substitute` (template symbols mostly miss) and `FormLine` (macro-generated forms always miss — and its ObjPtr keys are the S3.1 recycling hazard besides) are miss-dominated hot paths. Collapse: one late-bound `Scripting.Dictionary` per table — `.Exists` is throw-free, no project references. First purchase if P-PROF's expand/emit dials run hot.
- **P-PRELUDE — parse the constant once.** The prelude is fixed text re-tokenized and re-parsed on EVERY transpile — including once per raw-row probe and once per macro probe at load. Cache the parsed prelude forms; a VBE edit wipes the cache by wiping everything (the invalidation this project already trusts). O(probes × prelude) → O(prelude).
- **P-PROBE — the incremental macro probe.** Each `macro:` line probe-transpiles ALL accumulated macro text plus itself — O(macros²). Sixteen macros: nothing; a 200-macro dialect library: a quadratic load. Collapse: probe the new macro against the already-parsed table — same gates, linear.
- **P-BULK — the end-user's pothole, unreachable by engine fixes.** A loop of per-cell Put sentences is the eternal chatty-I/O sin; `with-fast-excel` mutes, never cures. Collapse at the LANGUAGE level: bulk verbs backed by Tier-2 array-slab runtime helpers (`vlaputrows` — one COM call per region) with English faces. Orders of magnitude, delivered as vocabulary — the grammarian's contribution to performance.
- *(Anti-item, recorded so it is never chased: the Run leg's ~200 ms of VBE injection + COM execution is machinery no one fixes — the design already single-shots it; the standing rule is merely that nothing per-run ever becomes per-row.)*

### The P-layer — ADOPTED AT THE OWNER'S WORD as the next session's OPENING work *(the bedtime list promoted: the owner's call is to ship these FIRST, both to refactor built-ins and to simplify the downstream items — L13's brackets lean on `when-let`/`try-else`/keywords, L14's `deflambda` consumes `quote` data and procedure docs, bucket 3's templates read through the predicates. Implementation order is dependency-aware, warmup to heaviest; every item ships with docstrings from birth, apropos-visible, pinned, goldens predicted — prelude-only items predict empty diffs, emitter items predict their exact shape)*

**Order of work: P.L1 → P.L7, then L.9/L.10, then bucket 3.**
- **✅ P.L1 — P-PRED — VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(the owner's word: "everything compiles and all tests/smokes pass" — taken as covering the loop's predictions: self-test 467/0, counters line unchanged at 102/16/199/5, both goldens empty, the owed standalone check, and the three maiden smokes including the `VlaApropos "?"` convention demo. The prelude's first expression-shaped macros are on the bench, documented, and pinned.)* *(prelude only; the warmup)*

  > **PL1.0 record (shipped blind — the machine has not spoken).** Touched: `VLA.bas` (L15.0 → **PL1.0** — prelude text only), `VLA_Tests.bas` (→ PL1.0). English, runtime, loader, IDE, build, dev rig: all untouched.
  > **The seven, one line each, docstrings from birth:** `zero?` → `(= x 0)`, `positive?` → `(> x 0)`, `negative?` → `(< x 0)`, `even?` → `(= (mod x 2) 0)`, `odd?` → `(<> (mod x 2) 0)`, `empty?` → `(isempty x)`, `blank?` → `(= (len (trim (& x ""))) 0)`. Facts verified in the uploaded source before writing (the read-the-prelude-not-memory rule generalized): `?` is an ordinary symbol character to the tokenizer (only parens, whitespace, `;`, `"` break tokens), so `?`-suffix names tokenize, key the macro table, and expand away before any identifier reaches VBA — `SymName` never sees one on a legal program. **The design note that earns its ledger line: these are the prelude's first EXPRESSION-shaped macros**, and their legality rests on a verified property of the walk — `ExpandMacros`' no-macro-at-head path rebuilds EVERY list element, so a predicate expands identically in a condition, a `set!`, or an argument position; the condition-position pin holds that property from now on. Four-question audit: one fixed parameter, no rest, no iteration, no surgery, no expansion-time decisions — the cleanest pass the audit will ever grade. **1:1 edges stated in the prelude comment rather than smoothed:** `even?`/`odd?` say whatever VBA's `Mod` says (fractional operands coerce, banker's rounding); `empty?` is `IsEmpty` verbatim; `blank?` is the spreadsheet cousin — Empty, Null, `""`, and whitespace-only all read blank via `(& x "")` coercion (Null & "" is "" by VBA's own rule) then `Trim`/`Len`, and a multi-cell range value type-mismatches at run time exactly as the raw VBA would. Bucket-3's condition templates read through these — the adoption rationale, exercised when G-items land.
  > **Ten pins (`TestPL1`):** the seven expansions byte-exact through `VlaExpandText` (each fits the printer's one line, so the goldens-through-VlaExpand rule gets the strong form, not frags); full-stack condition-position emission (`If ((x Mod 2) = 0) Then` — frag derived from `EmitChain`'s parenthesize-and-`Mod`-display paths, the L8.1 verified-emission rule honored by reading the code since the writing is blind); composition inside another template's test slot (`when` + `blank?`, the frag exercising generic-call rendering `len(trim((v & "")))`); and the docstrings on the bench — a name-hit (`zero? - true when the value is 0`) and a docstring-hit (`whitespace` finds `blank?`), both on this pass's own specimens per the borrowed-specimen lesson.
  > **Expected deltas from the printed baselines:** self-test **+10** (457 → **467**); the IDE Reload's counters line **unchanged**: `loaded: 102 rules, 16 macros, 199 tests (5 expected fails) from spreadsheet.vocab` (no vocab file touched; prelude macros live in the core's table, not the vocab registries — the U.9 diagnosis's own finding); **both goldens diff EMPTY** (prelude macros prepend at tokenize and never emit; no corpus sentence and no vocab template speaks a predicate yet); VerifyReport H1:H31 unchanged. No dial predictions — seven one-line templates are parse-cost noise.
  > **Verification loop:** `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **467 / 0**) → Reload (counters line unchanged) → Run `english.txt` → `VerifyReport` (H1:H31 green) → `VlaWriteGoldens` + `git diff` (**predicted: both goldens diff empty**; any diff is a stop-and-report finding) → **standalone-topology check owed** (`VLA.bas` ships): dev workbook closed, fresh workbook, built add-in, Check + Run a program — no state, but the module ships, so the check is owed. Maiden smokes, one line each through the instrument chain: `VlaTryValue "(blank? """")"` (expect `=> True`); `VlaTry "(dim x) (set! x 4) (when (even? x) (debug-print ""four is even""))"` (expect the line printed, then *(ran and cleaned up)*); and the discoverability demo `VlaApropos "?"` (expect all seven predicates — the `?` suffix is now itself a searchable convention). **Ask apropos between the Reload and any VlaTry** (the S5.1 wipe), and reload the vocabulary after the smokes. No reload-list change; no new module.
- **✅ P.L2 — P-WHENLET — VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(the owner's word: "everything compiles and all tests/smokes pass" — taken as covering the loop's predictions: self-test 473/0, counters line unchanged, both goldens empty, the owed standalone check, and the three deterministic smokes. The binding-conditionals are on the bench, documented, pinned, and consuming P.L1's own predicate — the P-layer's first self-composition, verified.)* *(prelude only)*

  > **PL2.0 record (shipped blind — the machine has not spoken).** Touched: `VLA.bas` (PL1.0 → **PL2.0** — prelude text only), `VLA_Tests.bas` (→ PL2.0). Everything else untouched.
  > **The two templates, exactly the ledger's signatures, C1 doctrine throughout** (caller names the binding, template dims it — `let-one`'s precedent — duplicate-Dim is the loud collision; values only, object bindings written by hand): **`(when-let x expr & body)`** → `(begin (dim x) (set! x expr) (if (not (empty? x)) (then body)))`; **`(if-let x expr then-body else-form)`** → same head with `(then then-body) (else else-form)` — then/else are ONE form each, multiples wrap in `(begin …)`, stated in the prelude comment. **The templates consume P.L1's `empty?` — the P-layer building on itself, on the ledger's own rationale** — so the fixpoint shows `isempty` one expansion deeper, and the L15 stepper shows the intermediate frame with the predicate still folded (free teaching material). **Semantics stated, not smoothed:** "something" is precisely `(not (isempty x))` — VBA's Empty (an unwritten cell's Value, an uninitialized Variant); Null IS something (a Null database field passes the gate), and so are 0 and `""`; a caller wanting the broader `blank?` gate writes the `if` by hand — one form, one meaning. Dim is procedure-scoped, so the binding OUTLIVES the body — recorded as a feature: after a `when-let`, x holds the value (or Empty) for later statements, exactly as the expansion says. Four-question audit: fixed positions, `& body` splices whole into one slot (when-let), no iteration, no surgery, no expansion-time decisions.
  > **Six pins (`TestPL2`):** when-let's fixpoint byte-exact (dim → set → guarded body, the pinned text showing `isempty` — the layering proof rides inside the byte pin); the multi-statement body splice byte-exact; when-let full-stack emission in order (`Dim w As Variant` → `w = q` → `If (Not isempty(w)) Then` → body → `End If` — frags from verified paths: the let-one pin's own `As Variant` frag, `EmitExpr`'s `(Not isempty(w))`); if-let's fixpoint byte-exact with both branches; if-let full-stack If/Else/End If order with both branch statements placed; and both docstrings on the bench by their words.
  > **Expected deltas from the printed baselines:** self-test **+6** (467 → **473**); counters line **unchanged** (`loaded: 102 rules, 16 macros, 199 tests (5 expected fails) from spreadsheet.vocab`); **both goldens diff EMPTY** (prelude-only; nothing in the corpus or vocabulary speaks a binding-conditional yet); VerifyReport H1:H31 unchanged. No dial predictions.
  > **Verification loop:** `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **473 / 0**) → Reload (counters line unchanged) → Run `english.txt` → `VerifyReport` (H1:H31 green) → `VlaWriteGoldens` + `git diff` (**predicted: both goldens diff empty**; any diff is a stop-and-report finding) → **standalone-topology check owed** (`VLA.bas` ships): fresh workbook, built add-in, Check + Run. Maiden smokes, deterministic in the Immediate window, one line each: `VlaTry "(when-let w 5 (debug-print (& ""bound: "" w)))"` (expect `bound: 5`); `VlaTry "(when-let w empty (debug-print ""never""))"` (expect only *(ran and cleaned up)* — the gate closing); `VlaTry "(if-let w empty (debug-print ""some"") (debug-print ""none""))"` (expect `none`); and optionally `VlaExpandStep "(when-let w q (debug-print w))"` — the narrated frames now include one showing `empty?` still folded, the P-layer's layering visible on the stepper. Ask apropos between the Reload and any VlaTry; reload the vocabulary after the smokes. No reload-list change; no new module.
- **✅ P.L3 — P-TRYELSE — VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(the owner's word: "everything compiles and all tests/smokes pass" — taken as covering the loop's predictions: self-test 478/0, counters line unchanged, both goldens empty, the owed standalone check, and the three smokes including the first error ever swallowed by the bench on purpose. The dim resolution stands unvetoed; when-let/try-else now cover messy sheets' two failure shapes — absence and error — as one form each.)* *(prelude only)*

  > **PL3.0 record (shipped blind — the machine has not spoken).** Touched: `VLA.bas` (PL2.0 → **PL3.0** — prelude text only), `VLA_Tests.bas` (→ PL3.0). Everything else untouched.
  > **The template:** `(try-else tmp expr fallback)` → `(begin (dim tmp) (on-error resume-next) (set! tmp expr) (if (<> err.number 0) (then (err.clear) (set! tmp fallback))) (on-error goto 0))` — the gathered definition's five error moves, executed on verified emitter facts: `(on-error resume-next)` and `(on-error goto 0)` are existing dedicated forms; `(err.clear)` rides the generic call path as `Call err.clear` (zero args, no parens — read from `EmitCallStmt`, the L8.1 rule); and arming ANY `On Error` form clears `Err` (VBA's own rule, `with-fast-excel`'s recorded comment), which is what makes the `err.number` check honest when expr succeeds. Four-question audit: three fixed positions, no rest, no iteration, no surgery, no decisions.
  > **One recorded RESOLUTION of the gathered sketch, open to the owner's veto: the template DIMS its binding** — the gathered expansion listed the five error moves without a `(dim tmp)`. Reasons, on the record: (a) **C1 consistency** — every sibling binding template (`let-one`, `swap!`, `returning`, `time`, `when-let`, `if-let`) dims, and duplicate-Dim is the doctrine's own promised loud collision; (b) **the failure class** — an undimmed temporary in generated code meets Option Explicit's untrappable compile modal during `Application.Run` (S4's physics), exactly the class the bench keeps retiring into words, and the dim-less spelling would walk every scratch user into it; (c) **nothing is lost** — assigning into an EXISTING variable stays expressible by hand (the five moves are five forms), while the binding idiom is the one the definition's own use case names ("the cell's number, or 0"). If the dim-less spelling is wanted instead, say so and PL3.1 drops it plus its pin line.
  > **Honest limits, stated in the prelude comment:** the fallback runs under the same `Resume Next` blanket — a failing fallback leaves the binding Empty and never raises; keep fallbacks simple (a literal, a bound name). And `On Error` state is procedure-scoped, not lexical: the closing `(on-error goto 0)` disarms ANY enclosing handler for the rest of the procedure (`with-fast-excel`'s recorded physics, same statement) — inside `with-error-handler`, re-arm after the form or keep the two apart.
  > **Five pins (`TestPL3`):** expansion frags in order through `VlaExpandText` (dim → arm → set → the inner if-form pinned as ONE flat frag, clear-then-fall-back order inside it — the full expansion exceeds the printer's one-line width, so frags-plus-order per the L7 precedent, reason recorded) ; full-stack emission of the six moves in order (`Dim w As Variant` → `On Error Resume Next` → `w = q` → `If (err.number <> 0) Then` → `Call err.clear` → `w = 0` → `On Error GoTo 0`); both slots substituting whole forms with the P-layer riding the expr (`(even? (zz))` → `w = ((zz() Mod 2) = 0)`, fallback `(+ 1 2)`); a second try-else RE-ARMING after the first disarms (the repeatability fact, positionally pinned); and the docstring on the bench.
  > **Expected deltas from the printed baselines:** self-test **+5** (473 → **478**); counters line **unchanged** (`loaded: 102 rules, 16 macros, 199 tests (5 expected fails) from spreadsheet.vocab`); **both goldens diff EMPTY** (prelude-only; nothing in the corpus or vocabulary speaks try-else yet); VerifyReport H1:H31 unchanged. No dial predictions.
  > **Verification loop:** `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **478 / 0**) → Reload (counters line unchanged) → Run `english.txt` → `VerifyReport` (H1:H31 green) → `VlaWriteGoldens` + `git diff` (**predicted: both goldens diff empty**; any diff is a stop-and-report finding) → **standalone-topology check owed** (`VLA.bas` ships): fresh workbook, built add-in, Check + Run. Maiden smokes, deterministic, one line each: `VlaTry "(try-else w (cint ""abc"") -1) (debug-print (& ""fell back to: "" w))"` (expect `fell back to: -1` — the first error ever swallowed by the bench on purpose); `VlaTry "(try-else w (cint ""42"") -1) (debug-print (& ""got: "" w))"` (expect `got: 42` — the happy path leaving no residue); `VlaTry "(try-else v (. (worksheets ""Nowhere"") name) ""missing"") (debug-print v)"` (expect `missing` — the definition's own messy-sheet shape). Ask apropos between the Reload and any VlaTry; reload the vocabulary after the smokes. No reload-list change; no new module.
- **✅ P.L4 — P-KW — VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(the owner's word: "everything compiles and all tests/smokes pass" — taken as covering the loop's predictions: self-test 488/0, counters line unchanged, both goldens empty as verified-then-predicted, the owed standalone check with the vocab's keyword templates exercised in the shipped topology, and the three smokes. The honest-scope finding stands: the mechanism was already general; the guard family and the pin coverage are what P-KW actually delivered.)* *(emitter)*

  > **PL4.0 record (shipped blind — the machine has not spoken).** Touched: `VLA.bas` (PL3.0 → **PL4.0**), `VLA_Tests.bas` (→ PL4.0). No vocab, English, runtime, IDE, or build touch.
  > **The scan the definition ordered, and its honest-scope finding (the L16 `dec!` class):** the general mechanism was **already here** — `EmitArgs` has turned `:kw value` pairs into `Named:=arguments` since early, and **all three call paths already ride it**: dot-member calls (`EmitDotText`), expression-position calls (`EmitExpr`'s generic path), and statement-position calls (`EmitCallStmt`). The vocabulary's `:password` (protect/unprotect-sheet), `:destination` (copy), and `:paste` (pastespecial) shapes were therefore **never hardwired special cases — they are the general mechanism's users, and there was nothing to refactor.** `(f :color red)` → `f(color:=red)` has been true in every position all along, with exactly ONE pin (the dot-member `:type` case) holding it.
  > **What P.L4 therefore delivers:** (1) **the guard family** — a keyword token OUTSIDE an argument list used to emit broken VBA silently (`SymName` passes `:` through) and meet the untrappable compile modal downstream (S4's physics); it now refuses at transpile time with words and directions, the L8-rider pattern applied to the `:` shape, at the four sites a stray keyword can land: **a value on its own** (`EmitExpr`'s atom path — safe by construction, since `EmitArgs` consumes legal keywords before any `EmitExpr` call), **the head of a statement** (`EmitCallStmt`, beside the operator rider), **the head of an expression** (`EmitExpr`'s call path), and **a member name after `.`** (`EmitDotText`). One message builder (`KwMisuseMsg`), teaching the correct spelling with the caller's own token: *"':color' is a keyword argument token - inside a call's argument list it pairs with the value after it ((f :color x) -> f color:=x); it cannot be …"*. Nothing legitimate ever reached the old paths — pure teaching gain, the rider's own precedent. **Recorded boundary:** NAME slots (`dim`/`label`/`goto`) keep raw behavior — a keyword there is unguarded, same as any other illegal identifier in those slots. (2) **The pin coverage the one-pin mechanism deserved** — see below. (3) The mechanism documented at `EmitArgs` as the P.L4 comment block, refactor finding included.
  > **Ten pins (`TestPL4`):** keyword args reaching expression-position calls (`x = f(color:=1)`), statement-position calls (`Call f(color:=1)`), positional-and-keyword mixing (`Call g(1, b:=2)`), keyword-name hyphen mangling (`:fill-color` → `fill_color:=3` — `SymName`'s own rule, verified), a keyword riding a **template parameter** into named emission (`when-let` carrying `(f :k 1)` — the P-layer composition case, and the fact P.L5/L13/G-item templates will lean on); the pre-existing missing-value refusal **pinned at last** (L12's "shipped then, pinned now" precedent); and the four new guards each by their words (a value on its own / the head of a statement / the head of an expression / a member name after `.`).
  > **Expected deltas from the printed baselines:** self-test **+10** (478 → **488**); counters line **unchanged** (`loaded: 102 rules, 16 macros, 199 tests (5 expected fails) from spreadsheet.vocab` — no vocab file touched, and the vocab's 199+5 proofs re-run green since their keyword templates emit byte-identically). **Both goldens diff EMPTY — the definition's "verify before predicting" clause executed:** there are no refactored call sites (nothing was hardwired), the guard only refuses shapes no corpus or vocab text contains (`english.txt` grep: zero keyword tokens outside argument lists — zero keyword tokens at all in raw rows), and legal emission bytes are untouched. VerifyReport H1:H31 unchanged. No dial predictions (four one-line guards are transpile-cost noise; the recorded transpile lead remains P-DICT, untouched).
  > **Verification loop:** `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **488 / 0**) → Reload (counters line unchanged) → Run `english.txt` → `VerifyReport` (H1:H31 green) → `VlaWriteGoldens` + `git diff` (**predicted: both goldens diff empty**; any diff is a stop-and-report finding) → **standalone-topology check owed** (`VLA.bas` ships): fresh workbook, built add-in, Check + Run — the vocab's protect/copy/paste keyword templates exercising the mechanism in the shipped topology. Maiden smokes, one line each: `VlaTry "(. (range ""a1"") copy :destination (range ""b1""))"` (expect *(ran and cleaned up)* and A1 copied to B1 in the active dev workbook — the general mechanism spoken from a scratch); `VlaTry "(set! x :color)"` (expect the worded refusal — the new guard teaching in the Immediate window, the modal retired before it ever fired); `?VlaFormat("(f :color red :sheet ws)")` (expect the form back canonically — keywords are ordinary symbols to the reader and formatter, demonstrated). Reload the vocabulary after the VlaTry lines. No reload-list change; no new module.
- **✅ P.L5 — P-QUOTE — VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(the owner's word across the cycle: compile clean, self-test 500/0, all tests pass, and all three smokes green — the second after its recorded in-place correction (the for-each header-list dialect; engine untouched, smoke authorship at fault, the verify-the-signature lesson recorded). Goldens empty as verified-then-predicted; the standalone check covered by the word. Data literals, the expansion boundary, and the reserved name are on the record; the `'` shorthand refusal stands re-openable.)* *(emitter + expansion-walk boundary)*

  > **PL5.0 record (shipped blind — the machine has not spoken).** Touched: `VLA.bas` (PL4.0 → **PL5.0**), `VLA_Tests.bas` (→ PL5.0). No vocab, English, runtime, IDE, or build touch.
  > **The form:** `(quote <datum>)` — an expression emitting ONE auditable VBA literal. A list becomes `Array(…)`; elements map by their **reader class**: string literals stay strings, numeric tokens stay numbers (the module's own `SymName` numeric convention, deliberately shared), and every other atom becomes its own text as a string **as written** — no `SymName` mangling (`(quote (hot-pink))` is `Array("hot-pink")` — data is not an identifier), no case folding, and keyword tokens are data too. Nested lists nest Arrays; a bare atom quotes by the same classes; the empty list is `Array()`. Statement position refuses with words and directions (the L8-rider pattern); arity ≠ 1 refuses teaching the wrap.
  > **The design decision the definition didn't ask for but correctness demanded — quote is a full data context, enforced at the expansion boundary:** without it, `(quote (when unless if))` — a word list — would have had its interior EXPANDED by the macro walkers (`when` is a macro name), silently corrupting data into code. Both walkers (`ExpandMacros` and `ExpandOnePass` — the L15 dual-site pattern) now **skip quote forms whole**; the skip is exact, not heuristic, because **`DefineMacro` refuses the name "quote"** with words (a macro so named could never fire — loud refusal over silent shadow), and it is budget-neutral and deterministic (skipping fires nothing, so L15's stepper arithmetic is untouched). The complementary fact, equally deliberate: **template parameters DO substitute inside a quote in a defmacro body** — `Substitute` runs when the enclosing macro expands, before the skip applies to the result — which is the template author's data-parameterization privilege (`(defmacro (pal c) (set! x (quote (c blue))))` works). Both sides pinned.
  > **The delegated shorthand judgment — `'` REFUSED for now, re-openable at the owner's word:** one character of sugar against a new tokenizer surface (`'` is currently an ordinary symbol character; splitting it off creates mid-symbol apostrophe edge cases and a second spelling of one form), in a language whose authors are template writers, not typists. `(quote …)` is the auditable spelling; `'` can arrive later as pure additive sugar without breaking anything written today. Clause-cited on the L5 four-question tradition: adjudicate publicly, refuse permanently-until-reopened.
  > **Recorded boundary (discoverability):** quote is emitter-privileged (lesson 12 — this transformation iterates over data and could never be a template), so it is invisible to `VlaApropos`, which lists macro-shaped things plus the runtime manifest. The P-layer's apropos clause is satisfiable only for macro-shaped items; the debt is recorded, and a static "emitter forms" apropos tier (quote, doc, at-line — one list, one pass) is a one-line adoption whenever wanted. **Honest limit in the code comment:** the literal emits on ONE line and VBA caps a logical line near 1,023 characters — a very large table belongs in cells (or a future `(include …)`), not a quote.
  > **Twelve pins (`TestPL5`):** symbol list → `Array("red", "green", "blue")` full-stack; reader classes preserved (`Array(1, 2.5, "txt")`); nested Arrays; bare atoms by class (`x = "red"`, `y = 5`); hyphens-and-case survival (`Array("hot-pink", "Red")`); keywords as data (`Array(":a", 1)` — the P.L4 guards provably not firing in data context); the empty Array; **expansion suppression on the prelude's most-expanded name** (`(quote (when 1 (f)))` → `Array("when", 1, Array("f"))`); **substitution-inside-quote** (`pal` quoting its parameter); the multi-datum refusal, the statement-position refusal, and the reserved-name refusal, each by its words.
  > **Expected deltas from the printed baselines:** self-test **+12** (488 → **500**); counters line **unchanged** (`loaded: 102 rules, 16 macros, 199 tests (5 expected fails) from spreadsheet.vocab`). **Both goldens diff EMPTY — the exact-shape prediction the definition ordered, verified before predicting:** the vocabulary contains zero `(quote` forms (grepped — its two "quote" hits are prose about quotation marks), `english.txt`'s raw rows speak no quote, and the expansion-walk skip changes nothing for programs containing no quote forms (the walk's output is byte-identical when the skipped head never occurs). VerifyReport H1:H31 unchanged. No dial predictions (two head-compares per list walk are noise; the transpile lead remains P-DICT).
  > **Verification loop:** `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **500 / 0**) → Reload (counters line unchanged) → Run `english.txt` → `VerifyReport` (H1:H31 green) → `VlaWriteGoldens` + `git diff` (**predicted: both goldens diff empty**; any diff is a stop-and-report finding) → **standalone-topology check owed** (`VLA.bas` ships): fresh workbook, built add-in, Check + Run. Maiden smokes, deterministic, one line each: `VlaTry "(dim a) (set! a (quote (red green blue))) (debug-print (ubound a)) (debug-print (a 1))"` (expect `2` then `green` — the first data literal ever spoken, zero-based as VBA's `Array` is); `VlaTry "(dim v) (for-each (v (quote (north south))) (debug-print v))"` (expect `north` then `south` — data driving a loop with no construction code; **CORRECTED during verification, at the owner's paste:** the original smoke wrote `(for-each v coll body)` and met `NthList`'s structural refusal — *"expected a list at position 2, got 'v'"* — because `for-each` takes a HEADER LIST exactly as `for` does: `(for-each (v coll) body)`. The engine behaved correctly and stays untouched — no code change, no version bump, no new pin (the incident is smoke authorship, the L7 correction's own class) — and the lesson extends the read-the-shape rule by one word: verify the SIGNATURE, not just the head; the head "for-each" was checked before shipping, its argument shape was not); `VlaTry "(quote (a))"` (expect the worded statement-position refusal naming VlaTryValue). Reload the vocabulary after the smokes. No reload-list change; no new module.
- **✅ P.L6 — P-SUBDOC — VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(the owner's word: compile clean, 511/0 after the PL6.1 pin fix, goldens empty, smokes green — with one recorded DEVIATION, adjudicated by the owner as an improvement and diagnosed from the IDE's source: `VlaApropos "stamp"` after Check listed the MACRO tier's `vla-stamp-cell` line, not the predicted program-sub line, because **Check translates and validates only — it never transpiles** (`EnglishIdeCheck` → `DoCheck` → `EnglishToVla`; the IDE's own header says so at line 24). My record's "the translate-once transpile runs last" was an unverified claim about a neighboring module's pipeline — the read-the-shape lesson's third form: signatures, then dialects, now pipelines. The machine behaved exactly as built. **Honest limitation now on the record:** in the IDE flow the program tier fills only at Run's compile step and is wiped moments later by injection, so today it serves Immediate-window transpiles — which the first three smokes proved working. A PL6.2/U-bucket rider — Check populating the table by transpiling its translation — is a natural adoption at the owner's word, not a silent slip-in.)* *(engine + apropos tier)*

  > **PL6.0 record (shipped blind — the machine has not spoken).** Touched: `VLA.bas` (PL5.0 → **PL6.0**), `VLA_Tests.bas` (→ PL6.0). No vocab, English, runtime, IDE, or build touch.
  > **The form:** `(sub tidy-report () "sort and re-band the report" …)` and `(function f () Long "answers" …)` — a string literal between signature (and return type, for functions) and body is documentation, on **L17's rule verbatim**: doc only when at least one more form follows; a lone string IS the body — and a bare string in statement position keeps meeting the emitter's **existing** worded refusal, so the rule adds no new silent path (behavior-preservation pinned). The doc never reaches `EmitBody` — invariance by construction, and the pin for it is byte-EQUALITY of a documented and undocumented transpile. **One adjacent fix the function shape demanded:** the return-type sniff now excludes string literals — before this pass, `(function f () "doc" …)` silently mangled the string into a garbage type name (`As _doc`-shaped); now a string at that position is a doc (with body) or the body's own worded refusal (alone). Worded paths only, recorded as the fix it is.
  > **The table and its lifetime, stated plainly (L17's flow, one table over):** `mSubDocs` — name AS WRITTEN (hyphens intact), doc or "", kind `sub`/`function` — is **reset by `VlaTranspile` only and populated at emission**, so it reflects the LAST program transpiled: after a Check that is the user's program (the translate-once transpile runs last); after a vocabulary load it is empty (macro probes transpile defmacro text, no procedures); after any Run or scratch the S5.1 wipe empties it with everything else. **The load-bearing design line: `VlaExpandText` never resets the table** — which is precisely what lets apropos's own prelude+carry refresh run inside every call WITHOUT wiping the program it is about to list (the L11.1 class of bug, designed out in advance this time and pinned). `VlaSubDoc(name)` is the public accessor — `""` for undocumented/unknown, case-insensitive, `GetMacro`'s Set-based lookup pattern (the Set-vs-Let hazard respected; `CollGet` Let-copies and is for values). Every procedure records, documented or not, so a user's whole program is browsable — the definition's pilot clause. Kind resolution recorded: the definition's "(program sub)" generalizes honestly to **"(program sub)" and "(program function)"**.
  > **Apropos:** a third tier between the macros and the runtime manifest — `name - doc (program sub)` / `name - (no documentation) (program function)` — pattern-matched over names AND docs like the macro tier. Existing apropos pins are presence pins and stand untouched.
  > **Eleven pins (`TestPL6`):** byte-equality invariance (one-line specimens so source-map tags agree and the comparison is exact); doc read-back with case-insensitivity; function type+doc together (`As Long`, `f = 1`); documented/undocumented/unknown each answering honestly; the type-sniff fix (`As Variant` when a doc stands where a type could); the untyped function's doc; the lone-string behavior-preservation refusal by its words; **last-transpile-wins** (alpha's doc gone after beta's transpile, before-state snapshotted in the detail); emission mangling vs as-written table (`Sub tidy_report(` / `tidy-report`); the apropos line by name, doc, and kind — **which simultaneously proves the refresh-cannot-wipe design**; and both kinds plus the no-documentation wording rendering on the tier.
  > **Expected deltas from the printed baselines:** self-test **+11** (500 → **511**); counters line **unchanged** (`loaded: 102 rules, 16 macros, 199 tests (5 expected fails) from spreadsheet.vocab`). **Both goldens diff EMPTY:** docstrings never emit (the byte-equality pin is the local witness), no corpus or vocab procedure carries one yet, and the type-sniff fix changes emission only for the string-at-type-position shape, which nothing existing contains (the vocabulary has no `(function` forms at all — its macros are dot-form templates; `english.txt`'s generated functions carry types from the English layer's own emission, untouched). VerifyReport H1:H31 unchanged. No dial predictions.
  > **PL6.1 — one-fail fix (tests only; SHIPPED into the cycle).** First run: 510 pass, one fail — the lone-string pin expected `SymText`'s *"expected a symbol, got a string literal"*, but the machine's printout named the ACTUAL refusal: **`EmitStmt`'s own atom guard, "bare atom used as a statement: '"only'"** — which fires at the statement dispatcher's entry, before any `HeadSym`/`SymText` path could. The guard is present in the pre-pass source, so the behavior-preservation claim was TRUE throughout; the pin quoted an inferred raise path instead of the verified one — **the L8.1 lesson (pin frags come from verified refusals, never from inferred paths) applied unevenly again, now applied here.** The code is correct and `VLA.bas` stays untouched at PL6.0; `VLA_Tests.bas` → **PL6.1**, the frag now matching the machine's own words. Expected on re-run: **511 / 0**, counts unchanged — the fix is in place, not additive.
  > **Verification loop:** `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **511 / 0**) → Reload (counters line unchanged) → Run `english.txt` → `VerifyReport` (H1:H31 green) → `VlaWriteGoldens` + `git diff` (**predicted: both goldens diff empty**; any diff is a stop-and-report finding) → **standalone-topology check owed** (`VLA.bas` ships): fresh workbook, built add-in, Check + Run. Maiden smokes, in this order: `?VlaTranspile("(sub greet () ""says hello"" (debug-print 1))")` (expect the VBA printed WITHOUT the doc — invariance visible to the eye), then `?VlaSubDoc("greet")` (expect `says hello`), then `VlaApropos "greet"` (expect `greet - says hello (program sub)` — the first program procedure ever listed on the bench); then Reload → **Check** `english.txt` in the IDE → `VlaApropos "stamp"` (expect `stamp - (no documentation) (program sub)` — the definition's pilot clause live: a user's own program browsable right after Check, no docstrings required). No injection in these smokes except the Check's own machinery — reload the vocabulary before any subsequent Run per the standing note. No reload-list change; no new module.
- **✅ P.L7 — P-INCLUDE — VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(the owner's word across the cycle: compile clean, 523/0 after PL7.1, goldens empty, all smokes green — the broken-lib smoke on the corrected colon-chained vehicle, its message verbatim, and the owner's screenshot carrying the map's first live sighting: `' vla:1` anchoring on an included statement beside `' vla:2` on a main line. Four chokepoints on the record; the escape-stack and smoke-instrument lessons standing. **THE P-LAYER IS CLOSED: P.L1–P.L7 all verified.**)* *(reader-level splice + line map)*

  > **THE LINE-MAP DESIGN (written before the code, per the definition's own instruction).** The whole item rests on one verified fact about the existing machinery: forms are tagged with **source-local** lines at parse (`lnRaw - mLineOffset`), and every reporting surface speaks that coordinate through exactly THREE chokepoints — `SrcLineTag` (reader-time errors, unbalanced parens included), the `emitfail` formatter in `VlaTranspile` (emit-time errors), and `MapTag` (runtime `' vla:N src:M` tags). So the design is: **splice at the text level, before tokenization, and build a per-line map** `mIncMap(k) = "label|fileLine|anchor"` for expanded-source line k — then consult the map at those three chokepoints and nowhere else. `label` "" = the main program; `fileLine` = the line within its OWN file (for main-file lines after an include, that is the main file's own numbering — the expanded line would be wrong there, a bug caught in self-review and fixed at both error surfaces before shipping); `anchor` = the main-file line of the include statement that pulled the block in, inherited through nesting. **Division of labor, stated:** transpile-time errors (reader AND emit) get the precise per-file coordinate — *"(near lib.vla line 2)"*, the definition's own example delivered at both surfaces — while **runtime tags anchor to the include's main-file line**, keeping the tag FORMAT and the runtime map's main-file coherence untouched (an included statement's runtime error points at the include line; recorded boundary, honest and coherent — included files hold whole procedures, and a runtime failure pointing at "the include that brought it" is the right grain for the English layer above). **Fast path:** a program with no `(include` substring never builds a map (`mIncMap Is Nothing`), and every consult site falls through to today's wording **byte-for-byte** — golden, pin, and dial safety by construction, pinned. **The splicer:** line-based, whole-line spelling only (`(include "name.vla")` alone on its line, leading whitespace allowed); the file name is RAW text (pre-tokenizer, no escapes; a name containing a quote is simply not an include line and meets the emitter's refusal instead). Relative names resolve against the active workbook's folder via `Application.PathSeparator`; absolute paths pass through — **the suite includes by absolute `%TEMP%` paths, so no pin depends on `ActiveWorkbook.Path`**. Includes nest (same recursion, inner file's labels win); depth caps at 16 with a cycle-suspicion message. A misplaced include form (inside a procedure, in an expression, sharing its line) refuses with one shared teaching message at three dispatcher sites. **Stated limit:** the splicer cannot know it is inside a multi-line string literal — an include-shaped line there will be spliced; do not spell includes inside multi-line strings. **Boundary:** `VlaTranspile` only — `VlaExpandText` does not splice (an include form there passes through expansion untouched, harmless), so expansion tools stay file-system-free.
  > **PL7.0 record (shipped blind — the machine has not spoken).** Touched: `VLA.bas` (PL6.0 → **PL7.0**), `VLA_Tests.bas` (PL6.1 → PL7.0). No vocab, English, runtime, IDE, or build touch. New machinery: `SpliceIncludes`/`SpliceWalk` (the map count IS the emitted-line count, so text and map stay aligned by construction, recursion included), `IsIncludeLine`, `ReadIncludeFile`, `IncLookup`, the `mIncMap` declaration, the three chokepoint consults, and the three refusal sites.
  > **PL7.1 — two-fail fix (core only; SHIPPED into the cycle).** First run: 521 pass, two fails, both diagnosed from the owner's paste plus a byte-level read of the shipped file. **Fail 1 — "Type mismatch (near vla line 1)" at the whole-line-rule pin:** the three misplaced-include refusal messages were generated through a shell-heredoc → python-string → VBA-literal escape stack, which landed a single literal backslash before each intended doubled quote. The quotes were therefore never adjacent, VBA's `""` doubling never applied, **the string literal closed early at `"(include \"` and the rest of the line parsed as integer divisions of strings — valid syntax, compiling clean, detonating as error 13 the moment the Case executed**, which `emitfail` then dutifully decorated. The postmortem was established by simulating VBA's string scanner over the exact file bytes (JSON display escaping had masked the backslash count; the simulator settled it). Fix: all three messages replaced with hand-spelled, **quote-free** text — `'(include ...)' splices a whole file and must stand alone…` — and verified by whole-line tokenization (strings all close, zero backslashes outside strings). The pin's frag is unchanged and now meets a real error-5 refusal. **New standing rule from the incident, alongside the welded-statement sweep: never manufacture VBA string literals through stacked escape layers — hand-spell them via direct edits — and the pre-present sweep now includes the string-scanner pass (quote parity alone was blind to this: the quote COUNT was even).** **Fail 2 — the unbalanced-parens message ignored the file map:** the raise formats its line phrase inline ("for the list opened at vla line N") — a **FOURTH reporting surface the design's "three chokepoints" claim missed**, and the owner's paste named it verbatim. Fix: `OpenedAtTag` — the same map consult, included files getting name + local line, main-file lines keeping the existing phrase with the main file's own numbering, fast path byte-identical. The design section above stands corrected: **four** chokepoints — `SrcLineTag`, `OpenedAtTag`, `emitfail`, `MapTag`. Touched: `VLA.bas` (PL7.0 → **PL7.1**); `VLA_Tests.bas` untouched at PL7.0 (both pins' frags were correct — they were the instruments that caught the machine, exactly their job). Expected on re-run: **523 / 0**, counts unchanged; all other P.L7 predictions stand as written, goldens-empty included (the fast path is untouched by both fixes).
  > **PL7.1 smoke-vehicle correction (recorded at the owner's screenshot; no code, no version change).** The broken-lib smoke produced the predicted message **verbatim** — *"unbalanced parentheses: missing ')' for the list opened at hello.vla line 1"* — but delivered it as VBA's runtime-error modal, because the smoke was authored as a bare `?VlaTranspile(...)`: an intentional raise with no handler pops the dialog. That is the raw call's physics, not a code fault — and it is the smoke-authorship family's third member (wrong dialect → wrong pipeline → wrong INSTRUMENT): an expected-error smoke belongs on a catching instrument, and every earlier refusal smoke rode `VlaTry`, which includes cannot (they are top-level; scratches wrap in a sub). **The corrected spelling for error-shaped transpile smokes in the Immediate window, colon-chained:** `On Error Resume Next: Debug.Print VlaTranspile("(include ""hello.vla"")" & vbCrLf & "(sub t () (hello))"): Debug.Print Err.Description: Err.Clear: On Error GoTo 0` — the error arrives as words on the next line, no modal. The screenshot also carried the map's first LIVE sighting on the happy path: `Debug.Print "from the library" ' vla:1` (an included statement anchoring to its include line) beside `Call hello ' vla:2` (a main line tagging itself) — the design's division of labor, photographed. **Optional rider at the owner's word, not shipped:** a small public instrument `VlaTryTranspile text` — print the VBA or the error in words, the `VlaTry` doctrine applied to transpile-shaped questions — would retire this whole smoke class; one short pass whenever wanted.
  > **Twelve pins (`TestPL7`), fixtures self-written under `%TEMP%`:** the lib's code transpiles in (`Sub lib_one(`, `Debug.Print 77`, `Call lib_one`); the include line never emits; **the runtime tag anchors to the include line** — the lib is two lines ON PURPOSE so `Debug.Print 77 ' vla:1` (lib line 2, anchor 1) proves the anchor override rather than a coincidence; an emit-time error names the included file and local line (`<path> line 2`); a main-file error AFTER an include names the main line with no file label (the resumption arithmetic, both sides asserted); the no-include baseline wording byte-stable (`(near vla line 2)` — the fast path); nesting delivers the inner file's code; a deep error names the INNERMOST file; a missing file refuses (`include: cannot read`); a self-including file refuses (`nesting deeper than 16`); an include inside a procedure refuses (`must stand alone on its own line`); and a reader-time unbalanced-parens error names the included file and line — the definition's example, pinned.
  > **Expected deltas from the printed baselines:** self-test **+12** (511 → **523**); counters line **unchanged** (`loaded: 102 rules, 16 macros, 199 tests (5 expected fails) from spreadsheet.vocab`). **Both goldens diff EMPTY:** the corpus and vocabulary contain no include lines, so the splicer takes its fast path on every existing program, the map stays `Nothing`, and all three chokepoints emit today's bytes — the invariance is structural, not incidental, and the baseline-wording pin is its local witness. VerifyReport H1:H31 unchanged. Dial note: one `InStr` per transpile on the fast path — noise; the transpile lead remains P-DICT.
  > **Verification loop:** `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **523 / 0** — note the suite writes seven small `vla_pl7_*.vla` fixtures under `%TEMP%`, ~200 bytes total, overwritten each run) → Reload (counters line unchanged) → Run `english.txt` → `VerifyReport` (H1:H31 green) → `VlaWriteGoldens` + `git diff` (**predicted: both goldens diff empty**; any diff is a stop-and-report finding) → **standalone-topology check owed** (`VLA.bas` ships): fresh workbook, built add-in, Check + Run. Maiden smokes: first write a lib beside your dev workbook — `hello.vla` containing `(sub hello () (debug-print "from the library"))` — then, since **includes are top-level and VlaTry scratches wrap in a sub, the honest smokes are transpile-shaped:** `?VlaTranspile("(include ""hello.vla"")" & vbCrLf & "(sub t () (hello))")` (expect the VBA with `Sub hello(` present and no include text — the splice visible to the eye); then delete the lib's closing paren and re-run the same line (expect *unbalanced parentheses … (hello.vla line 1)* — a FILE named in an error for the first time); then `?VlaTranspile("(include ""no-such.vla"")" & vbCrLf & "(sub t () (debug-print 1))")` (expect `include: cannot read 'no-such.vla' (looked at '<your workbook folder>…')`). Restore the lib after. Reload the vocabulary before any subsequent Run. No reload-list change; no new module.

The seven definitions, as gathered:

- **P-KW — keyword arguments as VBA named arguments.** `(f :color red :sheet ws)` → `f color:=red, sheet:=ws`. Not sugar — VBA's OWN named-argument feature, currently unreachable except in hardwired forms (`protect-sheet`'s `:password` proves the token shape exists). 1:1 to the byte. *For grammarians:* templates call optional-heavy helpers legibly without positional padding; *for end-users:* English rules bind slots to named args, so a rule author never counts commas.
- **P-TRYELSE — `(try-else tmp expr fallback)`** → `(begin (on-error resume-next) (set! tmp expr) (if (<> err.number 0) (then (err.clear) (set! tmp fallback))) (on-error goto 0))`. The Lisper's `(or (ignore-errors x) default)` as a pure C1 template. *For end-users:* graceful defaults over messy sheets — "the cell's number, or 0" — the single most-requested resilience idiom in spreadsheet automation, one form.
- **P-WHENLET — `(when-let x expr & body)` / `(if-let x expr then-body else-form)`.** Clojure-school binding-conditionals: dim, set, run the body only when the value is something (`(not (isempty x))`). Pure templates, caller-named binding. *For grammarians:* the "if the cell has a value, use it" pattern that today costs three forms costs one, and reads as intent.
- **P-PRED — the predicate prelude.** `zero?`, `positive?`, `negative?`, `even?`, `odd?`, `empty?`, `blank?` — one-line templates (`(defmacro (zero? x) (= x 0))` … `(even? x)` via `mod`), the `?`-suffix naming Lisp made famous. *For grammarians:* conditions in rule templates read as sentences (`(if (blank? (range {r})) …)`); *for the bench:* docstringed predicates make apropos a phrasebook of tests.
- **P-QUOTE — quoted data literals.** `(quote (red green blue))` (reader shorthand `'(...)` optional) → `Array("red", "green", "blue")`. One form, one VBA expression, fully auditable — data as literal instead of data as construction code. *For grammarians:* lookup lists, menus, and header rows live IN templates and splice whole; *for end-users:* list-valued settings without loops.
- **P-SUBDOC — procedure docstrings (L17 extended).** `(sub tidy-report () "sort and re-band the report" …)` — the doc stripped exactly as defmacro's, stored in a transpile-time table, listed by apropos as `tidy-report - … (program sub)`. *For grammarians:* vocabulary ACTION subs self-document; *for the pilot:* a user's own program becomes browsable.
- **P-INCLUDE — `(include "lib.vla")`.** Lisp's `load` as a reader-level textual splice: the file's forms enter at the include site, line-mapped to their own file (`lib.vla line N` in errors — the honest cost, and the flagged design work). *For grammarians:* shared VLA libraries ship as files beside vocabularies; dialects compose by inclusion, not copy-paste.

- **✅ L.9 — L13: the `with-` family growth — VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(the owner's word: compile clean, 531/0, goldens empty, all smokes green. The restore family is on the bench, the P-layer's first consumer, its composition doctrine — keywords in plumbing, binding forms in bodies — pinned and recorded.)* *(prelude only)*

**Bucket-3 addition at the owner's word — G.6 — G12: "Use library" (adopted; not yet slotted into the session order):** the English entry point for P-INCLUDE — `Use library "common.vla".` (surface variants for G-work to audition: `Use library common.` bare, per the bare-reference doctrine). **The wiring design, recorded now so the pass is an execution:** (1) **placement** — the rule cannot be an ordinary sentence rule, because sentence templates splice into `main`'s body and the pre-tokenizer splicer would inject whole procedures inside a sub; the English emitter needs a small TOP-MATTER channel (like the carried-defmacro tail, but at the head): library sentences collect during translation and emit as top-level `(include "…")` lines ABOVE `main`, one per library, first-mention order. (2) **Check learns to transpile** — the PL6.2 rider folds in here: after a clean translation, Check runs `VlaTranspile` on its own output (Windows-only note inherited from Run's VBProject clause does NOT apply — transpile is pure text, Mac-safe), which (a) surfaces include errors — missing file, `lib.vla line N` — at CHECK time where they belong, and (b) populates the P.L6 sub-doc table, making an imported library's procedures apropos-browsable right after Check, the "import a phrasebook and read it" story. (3) **path resolution** — relative names resolve against the workbook folder (the core already does this); the rule's teaching refusal for a missing library quotes the core's own `include: cannot read` message. (4) **proofs** — vocab `test:` lines pin the emitted include line and its top-matter placement; goldens move only when the corpus gains a library sentence (not before). MEDIUM — one emitter channel + one rule + the Check transpile; slots at the owner's word.

**DF1 — the dogfood pass — ✅ VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(the owner's word: compile clean, 531/0, all tests/smokes/diffs pass — the goldens moving exactly as predicted (three `.vla` sites, one `.vba` site), and the owner's screenshot showing `(blank? (range "b2"))` live at the seam. The generated VLA now speaks the P-layer; the leave-alones carry their reasons.)*

  > **DF1.0 record (shipped blind — the machine has not spoken).** Touched: `VLA_English.bas` (L12.0 → **DF1.0**), `spreadsheet.vocab` (five `test:` expecteds rewritten in lockstep), `VLA_Tests.bas` (L13.0 → **DF1.0** — one frag). `VLA.bas` deliberately untouched at L13.0.
  > **The scan, whole-codebase, findings first:** the refactorable old-Lisp shapes live almost entirely in ONE place — `ParseCondSimple`, the English layer's condition builder, which hand-assembles comparison plumbing as strings. The five refactors, each through the P-layer: **is empty** → `(blank? l)`; **is not empty** → `(not (blank? l))`; **is divisible by** → `(zero? (mod l r))`; **contains** → `(positive? (instr …))`; **does not contain** → `(zero? (instr …))`. The generated VLA now says what the sentence means. **VBA-invariance adjudicated per shape:** divisible/contains/does-not-contain expand to byte-identical VBA (zero?/positive? are the old comparisons one expansion down); is-empty/is-not-empty emit blank?'s own len-trim spelling — the pass's ONE deliberate VBA change, semantically equivalent for every value (`Trim(s)=""` ⇔ `Len(Trim(s))=0`; Null coerces through `& ""` identically), predicted exactly below. "starts with" (`instr … = 1`) has no predicate and stays as spelled.
  > **Adjudicated leave-alones, recorded so the next scanner inherits the reasons:** the **prelude was born clean** — P.L2's binding-conditionals already consume `empty?`, and no other prelude template tests emptiness or parity (consistency by construction, not luck); the **showalldata on-error pair** in the vocab is an ignore-errors STATEMENT — `try-else` binds a value and does not fit, and a statement-shaped `with-ignored-errors` bracket would be a NEW feature to adopt, not a refactor to slip in (recorded as an L-candidate); the **English `Try:` block** is statement-level recovery, not expression-binding — not a `try-else` shape; **`english.txt` stays untouched** — the corpus is the regression instrument and its primitive spellings must keep working forever (both spellings are legal; the corpus proves the old one still is); **VLA_Tests' pinned expansions stay** — pins hold history, and TestPL1's byte pins are exactly where the plumbing spelling is SUPPOSED to appear.
  > **Proof discipline (the G11 precedent executed):** the five vocab `test:` expecteds rewrote by the same textual rule as the builder, so template and proof cannot drift; the loader re-proves all 199 at every Reload — those rewritten lines ARE this pass's pins, plus the one core-suite frag (`AssertEnglish "is empty"` now expects `(blank? x)`; the frag follows the machine). Self-test COUNT unchanged.
  > **Expected deltas from the printed baselines:** self-test **531 / 0** (count unchanged — one frag edited, none added); counters line **unchanged**: `loaded: 102 rules, 16 macros, 199 tests (5 expected fails) from spreadsheet.vocab` (test TEXT changed, counts identical). **The `.vla` golden diffs at EXACTLY three corpus sites** — "is divisible by 2" → `(zero? (mod counter 2))`, "contains ""ok""" → `(positive? (instr 1 (cells check-row "f") "ok" vbtextcompare))`, "the problem is not empty" → `(not (blank? …))` — **plus nothing else. The `.vba` golden diffs at EXACTLY one site** — the is-not-empty If line inside the Try recovery, old `If (trim((X & "")) <> "") Then` → new `If (Not (len(trim((X & ""))) = 0)) Then` — **plus nothing else** (the other two sites expand to identical bytes; a second `.vba` site moving is a stop-and-report finding). VerifyReport **H1:H31 unchanged** (equivalence argued above; H11's rescue path exercises the changed line). No dial predictions.
  > **Verification loop:** `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **531 / 0**) → **Reload** (counters line unchanged — and the Reload itself is the pass's main proof: 199 tests including the five rewritten ones must pass or the vocabulary refuses to load) → Run `english.txt` → `VerifyReport` (H1:H31 green) → `VlaWriteGoldens` + `git diff` (**predicted: `.vla` three sites, `.vba` one site, exactly as spelled above**) → **standalone-topology check owed** (`VLA_English.bas` ships): fresh workbook, built add-in, Check + Run. Maiden smokes, one line each: Reload, then `?EnglishToVla("If cell B2 is empty, log ""blank"".")` (expect `(blank? (range "b2"))` inside the If — the dogfood visible at the seam); `?EnglishToVla("If counter is divisible by 2, log ""even"".")` (expect `(zero? (mod counter 2))`); then Check + Run `english.txt` and read H11 (expect `rescued`, the changed VBA behaving identically). Reload the vocabulary after any scratches. No reload-list change; no new module.

  > **L13.0 record (shipped blind — the machine has not spoken).** Touched: `VLA.bas` (PL7.1 → **L13.0** — prelude text only), `VLA_Tests.bas` (→ L13.0). Everything else untouched.
  > **Three brackets, each on with-fast-excel's proven restore-label pattern VERBATIM** (arm → body → fall through the label → restore → re-raise guard → trailing disarm; C1 caller-named labels; duplicate-label is the loud collision): **`(with-screen-off restore-label & body)`** — the definition's first named bracket, with-fast-excel's single-knob sibling (screen only, calc untouched; restores to True, the same contract as the English "Turn on screen updating."); **`(with-no-alerts restore-label & body)`** — the family's growth pick: DisplayAlerts off around the body, restored True on BOTH exits, because leaving alerts globally off is the classic automation injury — which is the whole reason it deserves bracket shape; **`(with-protected-sheet ws pw restore-label & body)`** — the definition's second named bracket: unprotect, body, RE-protect on both exits, **P.L4's keyword mechanism riding inside a prelude template** (`:password` pairing at both call sites) — the session order's "build the brackets USING the P-layer" executed where natural. **Honest limit stated (check='s clause, same class):** `ws` and `pw` each appear twice in the expansion, so side-effecting expressions there evaluate twice — bind with `let-one` first; an error while re-protecting propagates raw (handler already active), with-fast-excel's inherited contract.
  > **One adjudicated refusal, recorded:** `with-existing-sheet` via `try-else` was considered and REFUSED — try-else is values-only by its own P.L3 record (`set!` cannot bind a Worksheet), and bending it would smooth what the doctrine says to state. The design line that replaces it, pinned: **the P-layer's binding forms compose INSIDE bracket bodies, not in bracket plumbing.** Also recorded: `when-let`/`try-else` proved natural in bodies, keywords in plumbing — the "where natural" clause adjudicated per form. Each bracket is a G5-auditioned English verb candidate ("Without alerts:", "On the protected sheet …:"), staged for bucket 3 — no vocab moves this pass.
  > **Eight pins (`TestL13`):** with-screen-off expansion frags in order (arm → body → label → restore → re-raise → disarm; frags-plus-order per the L7 precedent, the multi-statement expansion exceeding one printed line); with-screen-off full-stack emission of the restore contract in order; with-no-alerts bracketing DisplayAlerts False → body → True; with-protected-sheet expansion order (unprotect → body → label → re-protect); with-protected-sheet emission with **`password:=pw` verified at BOTH call sites positionally** (the second-occurrence search explicitly guarded — `InStr` raises on a start of 0 and VBA's `And` does not short-circuit, a hazard now noted in the pin itself); the P-layer composition pin (`when-let` inside a bracket body, its guard emitted between arm and label); two brackets with distinct labels coexisting in one procedure (the C1 promise held); and all three docstrings on the bench.
  > **Expected deltas from the printed baselines:** self-test **+8** (523 → **531**); counters line **unchanged** (`loaded: 102 rules, 16 macros, 199 tests (5 expected fails) from spreadsheet.vocab`); **both goldens diff EMPTY** (prelude-only; nothing in the corpus or vocabulary speaks the new brackets). VerifyReport H1:H31 unchanged. No dial predictions.
  > **Verification loop:** `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **531 / 0**) → Reload (counters line unchanged) → Run `english.txt` → `VerifyReport` (H1:H31 green) → `VlaWriteGoldens` + `git diff` (**predicted: both goldens diff empty**; any diff is a stop-and-report finding) → **standalone-topology check owed** (`VLA.bas` ships): fresh workbook, built add-in, Check + Run. Maiden smokes, one line each: `VlaTry "(with-no-alerts done (debug-print ""inside the bracket""))"` (expect the line, then *(ran and cleaned up)* — alerts restored by the time the prompt returns); `VlaTry "(with-screen-off done (when-let w 5 (debug-print (& ""bracketed bind: "" w))))"` (expect `bracketed bind: 5` — the P-layer inside a bracket, live); `VlaApropos "restored"` (expect with-fast-excel and BOTH new restore brackets listed by their shared docstring word — the family visible as a family). The protected-sheet bracket needs a passworded sheet to smoke honestly — optional, at your leisure: on a scratch sheet protected with password "x", `VlaTry "(with-protected-sheet activesheet ""x"" done (set! (. (range ""a1"") value) ""wrote through""))"` (expect the write to land AND the sheet protected again after). Ask apropos between the Reload and any VlaTry; reload the vocabulary after the smokes. No reload-list change; no new module.

**G.6 — G12: "Use library" — ✅ VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(the owner's word after the G12.1 cycle and the residue cleanup: everything compiles, all tests/smokes pass — 537/0, the six-step maiden story green, TopoGuard speaking at Check, and the owner's final screenshot showing the build-error dialog carrying `hello.vla line 1` with the orienting sentence. The library story is closed end to end: sentence → bottom-matter splice → browse via apropos after Check → both failure modes in words.)*

**DR1 — the reload consolidation — ⛔ REVERTED AT THE OWNER'S WORD (crash unchased; the item stays adopted, parked).** *First on-machine run hard-crashed Excel. `VLA_DevRig.bas` reverted byte-identical to the verified L8.2 (diff-confirmed against the owner's upload). The bug is deliberately unchased at the owner's word; suspects recorded for whoever reopens it: (a) the CommandBar ID-578 `Execute` — driving the VBE's own Compile control from code RUNNING INSIDE the project being compiled, immediately after component churn with deferred removals pending, is the prime suspect (the VBE re-tokenizes mid-execution); (b) `Set Application.VBE.ActiveVBProject` during the same churn; (c) `DeleteLines` on the executing project's own components. The lesson banked without the chase: programmatic VBE COMPILE is a different beast from programmatic component replace (which the rig has done safely for eight versions) — a reopened DR2 should stage the compile as the LAST act via `Application.OnTime` after the sub returns and removals settle, or drop the compile step and keep sweep+selftest+goldens. Parked until called.*

**L.10 — L14: `deflambda` — ✅ VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(548/0, goldens empty, the cell-level smokes green — `=double(21)` answering 42 from a Lisp scratch. **BUCKET 2 IS CLOSED.** One verification note diagnosed, no code at fault: the owner's IDE raw-row attempt `(deflambda double (x) ""twice the input"" (* x 2))` refused with "got 5 forms" because the DOUBLED quotes belong to the Immediate window's VBA string escaping, not to VLA — in a raw row the doc is plainly quoted, `"twice the input"`, and the doubled spelling tokenizes as an empty string plus three bare words plus another empty string, which is exactly the five extra forms the refusal counted. **deflambda works everywhere VLA runs — raw rows, scratches, libraries — with each venue's own quoting.** Also confirmed on the record, because it is the feature's whole point: **registered Names are workbook DATA, not VBA state** — they save with the file, survive every VBProject wipe (wipes reset VBA variables; Names live beside cell values), and keep working in workbooks with no Frazaro and no macros enabled, until overwritten or removed in Name Manager. The recorded Undo limit stands.)*

**L18 — TCO for VLA functions + the bricks library — ✅ VERIFIED ON-MACHINE AND MARKED COMPLETE AT THE OWNER'S WORD** *(557/0 after L18.1; goldens empty; the stack never noticed. THE SESSION'S WORK IS COMPLETE — Alpha 5 closes; Alpha 6 carries the remainder.)* *(core + tests + the new corpus file `hello.vla`; carries the L14.1 inner-lambda rider)*

  > **L18.0 record (shipped blind — the machine has not spoken).** Touched: `VLA.bas` (L14.0 → **L18.0**), `VLA_Tests.bas` (→ L18.0), **NEW corpus file `hello.vla`** (the third of the family, beside `english.txt` and `spreadsheet.vocab`). Adoption noted: L18 was a chat-proposed item; the owner's order this turn is its adoption call.
  > **HOW TCO EXISTS ATOP A RUNTIME THAT LACKS IT — the owner's question, answered in the mechanism:** VBA gives no tail-call elimination, but the TRANSPILER owns the emitted shape — and `(return (self args…))` is a tail call **by construction**, because `return` exits. So the emitter rewrites every self-return into **rebind-and-jump**: parameters reassigned, `GoTo vla_tco` to a label at the body's head. Recursion in the language, a loop in the metal. The law, stated in the code comment: armed only for **functions whose every parameter is `(byval …)`** (rebinding a ByRef parameter would mutate the ORIGINAL caller's variable — a semantics change, so ByRef functions recurse normally: correctness over optimization, stated not smoothed); **all argument expressions evaluate into `vla_tco_N` temps BEFORE any parameter reassigns** (cross-referencing calls like `(self b (- a 1))` stay exact; one parameter rebinds directly, no temps); **non-tail self-calls keep true recursion** (`(return (+ 1 (self …)))` — the self-call is not the return's direct operand); the `vla_tco*` names are emitter-reserved like `vla_step` (emitter privilege, not template law); and **a function with no self-return emits byte-identically to before** — the goldens' own guarantee. English-defined recursive actions inherit the transform for free whenever their emission matches the shape.
  > **THE PERFORMANCE TRUTH, precisely:** TCO changes **stack space** from O(n) frames to **O(1)** — which is what removes the depth ceiling entirely (200,000 iterations run where plain recursion dies in the low thousands of VBA frames). **Time complexity does not change class** — O(n) work is O(n) work — but the constant factor drops (no call/frame overhead per step), so yes: faster than the VBA-recursive counterpart, and *unboundedly deeper*, but not a big-O time win. The owner's instinct about crashes was exactly right for the recursive spelling; TCO is the spelling that doesn't crash.
  > **THE BRICKS (`hello.vla`) — the owner's thesis, endorsed and executed:** the owner is **not wrong** — pre-registered named LAMBDAs are exactly how "automated automations" happen: logic that today needs a macro because the formula would be unwritable becomes a CELL formula once the abstraction exists by name, which is Spreadsheet Lisp's own lesson applied from the VLA side (the kinship is noted with respect — formula-simplification-through-function-abstraction is precisely this feature's theory). Five bricks ship, each documented, each an iteration-shaped escape from the LAMBDA recursion ceiling: `sum-to` (REDUCE over SEQUENCE), `running-total` (SCAN), `double-all` (MAP), `count-positive` (**`positive?` on a sheet** — the P-layer brick), `clamp`. The file is **deliberately multi-line** and is the standing proof for the owner's last question: **the VLA reader has always been line-agnostic in files** — the goldens themselves are multi-line — and the one-form-per-row rule lives ONLY in the IDE's L0 raw-row scanner (parens-terminated single rows, a deliberate English-layer rule). **Proposed, awaiting the owner's word: L0.2 — multi-row raw forms** — the IDE's raw scanner continues a form across rows while its parens stay unbalanced (the L12 balance counter already exists to power it); MEDIUM-LOW. **Enabling machinery shipped as the L14.1 rider:** the formula dialect gains **inner lambdas** — `(lambda (params) body-expr)` → Excel's `LAMBDA(p, body)` — without which REDUCE/SCAN/MAP calls could not carry their function arguments (the params list would have parsed as a call).
  > **L18.1 — compile fix (core only; SHIPPED into the cycle).** The compile gate caught it exactly as designed: *"ByRef argument type mismatch"* at `HeadSym(pv)` — `HeadSym` (and `Nth`) take a TYPED `Collection` ByRef, and a `Variant` cannot pass into a ByRef typed parameter. The fix is the walkers' own `Set lst = d` pattern applied to the arming block's two call sites (a typed `pvl` local, `Set` first) — a pattern my `TcoScanForm` and `EmitReturn` already used, missed in the one block between them; the audit sweep over every added line confirms no sibling site remains. `VLA.bas` → **L18.1**; tests untouched. Expected on re-run: compile clean, **557 / 0**, all other predictions standing.
  > **Nine pins on the happy path** (`TestL18` six, `TestHelloLib` three): the rebind-and-jump shape in order WITH the absence of any call-shaped self-reference; the docstring riding the transform; the single-parameter no-temps path; non-tail recursion preserved; ByRef disarming; the arity refusal by words; the bricks registering through a real include (`sum_to`'s Add line with the full `reduce(0, sequence(n), LAMBDA(acc, i, (acc+i)))` formula); the P-layer brick (`IF((v>0), 1, 0)`); an imported brick's doc on the bench with the worksheet-function kind. `hello.vla` located by the corpus prober; **its absence is a failure** — the corpus-family contract.
  > **Expected deltas:** self-test **+9** (548 → **557**); counters line **unchanged**; **both goldens diff EMPTY** (nothing in the corpus recurses, and the TCO emitter changes no byte for non-recursive functions — structural, pinned by the goldens themselves). VerifyReport H1:H31 unchanged.
  > **Verification loop:** place `hello.vla` beside the corpus files → `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **557 / 0**) → Reload → Run `english.txt` → `VerifyReport` → goldens (**both empty**) → **standalone check owed** (core ships). Maiden smokes: `VlaTry "(function fi ((byval n Long) (byval acc Long)) Long (if (<= n 1) (then (return acc)) (else (return (fi (- n 1) (* n acc)))))) (debug-print (fi 170 1))"` — a depth that laughs at stacks (expect the astronomically large Double... use `(fi 20 1)` for a readable `2432902008176640000`-scale answer; the POINT smoke: `(fi 100000 1)` returns without a stack error — overflow of the NUMBER, never the stack); then the bricks live: IDE two rows `Use library "hello.vla".` / `(register-bricks)` → Check → Run → in any cell `=sum_to(100)` (expect **5050** — a fold with no VBA and no recursion), `=clamp(99, 0, 10)` (expect 10), `VlaApropos "bricks"` after a Check (the imported library browsable). Reload the vocabulary after scratches.

  > **L14.0 record (shipped blind — the machine has not spoken).** Touched: `VLA.bas` (G12.1 → **L14.0**), `VLA_Tests.bas` (→ L14.0). Dev rig REVERTED to L8.2 in the same shipment (above). Nothing else moves.
  > **The definition, delivered:** `(deflambda name (params) ["doc"] body-expr)` — a STATEMENT emitting one auditable VBA line that registers an Excel `LAMBDA` under a workbook Name: `ThisWorkbook.Names.Add Name:="tax", RefersTo:="=LAMBDA(x, (x*0.2))"` — **the worksheet function itself contains zero VBA**; sheets call `=tax(A1)` with no macros in the loop. Two Lisps shake hands: the body is a **formula dialect** of VLA expressions — chain operators compact-and-parenthesized, `mod` → `MOD(a, b)` (two operands, more refuses), `and/or/not` → the `AND/OR/NOT` functions, `if` in the THREE-ARG spelling (`(if test then-value else-value)` → `IF(t, a, b)`; the statement-if's `(then …)` blocks refuse teaching exactly that), generic calls → `FN(args)` as written. **The bench consumption, exactly as the session order promised:** macro expansion runs before formula emission, so **`(even? n)` in the source reaches the sheet as `MOD(n, 2)=0`** — the P-layer inside a worksheet formula; `(quote (1 2 3))` becomes the array constant `{1,2,3}` (P.L5's data classes, formula-flavored — a quoted symbol is a string, never a reference; nesting refuses, Excel arrays don't nest); the docstring rides **both** P.L6's table (apropos: `tax - the vat (worksheet function)` — `RecordSubDoc`'s kind widened from Boolean to the string it always wanted to be) **and the Name's own `.Comment`**; and the L15 stepper narrates the expansion like any form, free. Names mangle by `SymName` (Excel refuses hyphens exactly as VBA does); the as-written name lives on the bench. **Guards, each by its words:** statement bodies (`(set! …)` named as a statement), the then-block teaching, nested quote, keyword tokens (formulas have no named arguments), multi-form bodies, top-level placement (`Names.Add` RUNS — put it inside a sub). **Stated limits:** re-registering replaces the formula (reruns safe); Undo's snapshots do not cover Names (a created worksheet function persists); on Excels without LAMBDA the Name registers but evaluates `#NAME?` — the registration cannot tell.
  > **Eleven pins (`TestL14`):** the Add line byte-frag with the exact RefersTo; multi-param comma-joining and infix comparison; **the load-bearing bench pin — source `even?`, sheet `MOD`**; the docstring on the Comment AND `VlaSubDoc`; the apropos line with the `(worksheet function)` kind; quote → `{1,2,3}`; hyphen mangling with the as-written bench name; and the four refusals by their words (statement body, then-blocks, nested quote, top level).
  > **Expected deltas from the printed baselines:** self-test **+11** (537 → **548**); counters line **unchanged**; **both goldens diff EMPTY** (nothing existing speaks deflambda; the kind-widening rewrites two internal call sites byte-neutrally and the P.L6 pins' wording is untouched). VerifyReport H1:H31 unchanged. No dial predictions.
  > **Verification loop (MANUAL VBE REPLACE FIRST for `VLA_DevRig.bas` — the REVERT must land before the old reload runs):** replace the rig by hand with the reverted L8.2 → `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **548 / 0**) → Reload → Run `english.txt` → `VerifyReport` → `VlaWriteGoldens` + `git diff` (**both empty**) → **standalone check owed** (`VLA.bas` ships). Maiden smokes — the showpiece deserves its demo: `VlaTry "(deflambda double (x) ""twice the input"" (* x 2))"` (expect *(ran and cleaned up)* — then type `=double(21)` INTO A CELL and read 42: a worksheet function born from a Lisp scratch, no VBA in it); `VlaApropos "twice"` (expect `double - twice the input (worksheet function)` — though note the S5.1 wipe: ask via a fresh transpile context, or Check a program carrying the deflambda and ask after); `VlaTry "(deflambda parity (n) (if (even? n) ""even"" ""odd""))"` then `=parity(7)` in a cell (expect `odd` — the P-layer answering from a sheet). Reload the vocabulary after the scratches. No reload-list change; no new module.

  > **DR1.0 record (shipped blind — the machine has not spoken).** Touched: `VLA_DevRig.bas` (L8.2 → **DR1.0**) — nothing else. `VlaDevReload` now runs the mechanical half of the verification loop in one call: **sweep → replace → compile → self-test → goldens.** (1) **The stale-artifact sweep:** `EN_*` standard modules (injected program modules — `EN_Sheet`, `EN_<tag>`) and `*_old` strandings (hazard 11) are removed at the top — the G12.1 residue class, which cost a diagnosis round, retired from the workflow. Two VBA facts shape the mechanics, both stated in the comment: component REMOVAL is deferred until the sub returns, but LINE deletion is immediate — so each stale module is **emptied first** (neutral for the in-sub compile) and removed second; names collect before removal (never remove while iterating). (2) **The same emptying now applies to the `_old` husks in the replace loop** — without it, the deferred husk still holds every procedure during the in-sub compile, and husk-vs-fresh-import duplicates are exactly the "Ambiguous name detected" failure `VlaAuditDuplicates` hunts; emptied husks compile as nothing. (3) **Programmatic Debug > Compile** via the VBE's own menu control (folklore ID 578), riding the VBProject trust the rig already requires — a compile error pops HERE at reload instead of ambushing later; a disabled control means already-compiled, and a missing control degrades to the manual instruction in words. (4) **`VlaSelfTest` then `VlaWriteGoldens`** — the goldens writer self-loads the vocabulary (verified in its source before chaining), so the reload ends **vocabulary-loaded and apropos-ready**. The git diff and the corpus Run stay deliberately manual: the diff is the owner's witness, and a Run wipes state by VBA's physics — a reload should not do that uninvited.
  > **Expected behavior on first use (no counts change — rig only, self-test stays 537):** manual VBE replace of `VLA_DevRig.bas` FIRST, then one `VlaDevReload` prints: any `swept stale artifact:` lines → seven `reloaded:` lines → `compile: invoked (no modal = clean)` (or `already compiled`) → the full self-test run ending **537 / 0** → the goldens block ending with the git-diff reminder → the DR1 summary line naming what remains manual. **Maiden smoke:** before the reload, hand-create a junk module named `EN_Test` containing one garbage line — expect `swept stale artifact: EN_Test` and a clean compile despite it; then run the reload a second time — expect zero sweeps, `compile: already compiled`, and the same 537/0.
  > **Owner's second question, answered on the record (and U.15 born from it):** the wipe is **VBA's own physics, not a Frazaro choice** — ANY modification of a workbook's VBProject (and every Run injects a module) resets execution state project-wide, all module-level variables to Empty; there is no keeping state across a code edit. So the fix is not avoidance but **cheap recovery**, in two parts. Part one exists as of G12: **for browsing, Check is now enough** — Check transpiles without injecting, so it populates the doc table and wipes nothing; the check-then-apropos workflow has no reload step at all. Part two is adopted below as **U.15**. **The third question, adjudicated from the owner's own screenshot:** the modal DOES carry the coordinate — *"…for the list opened at hello.vla line 1"* — and the line named is the OPENER of the never-closed list, deliberately: a missing closer has no line of its own (the file simply ends), so the opener is the actionable coordinate ("this list never closed"). For a long library the distance between opener and end can obscure that; a micro-rider — appending "(the file ends at line N without closing it)" to the unbalanced message — is one line in the reader plus one pin, at the owner's word.

- **⬜ U.15 — lazy vocabulary self-heal after state wipes** *(adopted from the owner's Q2; slots at the owner's word)*: the human-facing instruments (`VlaApropos`, `VlaTry`/`VlaTryValue`, the L11 tools) detect the post-wipe empty state and **reload the vocabulary themselves** from the workbook's known vocab path before answering, printing one honest line — *"(vocabulary reloaded after the run's state wipe)"* — so the run-then-explore workflow needs no manual reload. Design constraints inherited from the records: the reload is the instruments' existing load path (the counters line prints, tests re-prove — a self-heal must not be a quieter load than a manual one), the S5.1 miss-wording stays for genuinely-nothing-loaded workbooks with no vocab file, and the DR1 reload already ends vocabulary-loaded, so the heal fires only after Runs. LOW-MEDIUM — one detection seam + the path resolution, across the instrument entry points.

  > **G12.0 record (shipped blind — the machine has not spoken).** Touched: `VLA_English.bas` (DF1.0 → **G12.0**), `VLA_IDE.bas` (L11.1 → **G12.0**), `VLA_Tests.bas` (→ G12.0). Core, vocab, runtime, build, dev rig: untouched.
  > **The sentence, and the wording adjudication the owner deferred:** surfaces are **`use|import` × `library|code`**, optional **`at|from`**, quoted name — so *"Use library "helpers.vla"."* and *"Import code from "kit.vla"."* are the same sentence. **"code" joins "library"** precisely for the owner's reason: the sentence should say it grabs CODE, not books; **"VLA" was dropped as jargon** — *Import code from…* says the same thing in words a non-programmer owns. (These surfaces are dispatcher-level gates, not vocab alternations — the gate fires only when the SECOND word is library/code, the To-get gate's own pattern, so sentences merely starting with use/import keep falling through to whatever owns them.) **The name must be quoted, and the refusal teaches why:** file names contain periods and a period ends a sentence — the spaced-sheet-name rule, one more time.
  > **The wiring, exactly as the adopted design specified:** (1) **top-matter channel** — library sentences collect wherever they appear (an import applies to the whole program) and emit as top-level `(include "…")` lines ABOVE everything, first-mention order, **case-insensitive dedup** (a double splice would define every library procedure twice and die at VBA's duplicate-definition step — spliced once instead, recorded); placement is safe by the L4.2 adjudication's own terms — the prefix exists only in programs that import, so no other program's source map moves by a line. (2) **Check now transpiles its own translation** — the PL6.2 rider folded in: pure text, Mac-safe (no VBProject touch), cheap at Check scale; include errors (a missing library, a broken `lib.vla line N`) surface AT CHECK with the transpiler's own P.L7-mapped words, and the message adds one orienting sentence ("if it names a library file, the problem lives in that file, not in these sentences") because a transpile failure is not row-attributable in general — no row is marked wrong. The same transpile **populates the P.L6 procedure-doc table**, so an imported library's procedures — and the program's own — are apropos-browsable right after Check: the "import a phrasebook and read it" story, live, and the P.L6 stamp-deviation limitation retired in the same stroke.
  > **Five pins (`TestG12`), full-stack through `EnglishToVla`:** the include emitted ABOVE `(sub main` (positional — the top-matter channel proven, not just presence); the `Import code from` surface; **repeat-splices-once** with deliberately case-varied spellings (occurrence count = 1); the unquoted-name refusal by its teaching words; and two libraries in first-mention order. One authoring correction made in self-review before shipping: the pins' filler sentence was `Log 1.` — decimal-ambiguous (a trailing period after a bare number belongs to the number machinery) — replaced with `Log "x".` in all five sources. The **Check-transpile hook is IDE machinery needing sheets** and is smoke-verified on-machine, per the standing rig/suite boundary: the suite pins the language, the smokes pin the panel.
  > **G12.1 residue note (adjudicated at the owner's second screenshot; no code change — ledger only).** The compile modal at reload — *"Only comments may appear after End Sub…"*, highlighting the misplaced `Dim` — is **the stale G12.0 injection, not G12.1's output**: the failed Run died at the untrappable modal BEFORE the injection lifecycle's cleanup (S4 physics), so the broken generated module remained in the program sheet's code-behind, and Debug → Compile compiles the whole project, residue included. Same bytes, same root cause, different compiler wording (whole-project compile reports the topology structurally; execution reported it as Option Explicit's "Variable not defined"). G12.1's generator is unaffected and has not yet run — the compile gate blocks everything while the wreckage stands. **Cleanup step, now part of this item's loop:** in the VBE, open the program sheet's code module (the one in the screenshot), select all, delete — the module is sheet-attached and cannot be removed, but emptied it compiles clean — then proceed: `VlaDevReload` → Debug → Compile (clean) → `VlaSelfTest` (**537 / 0**) → the G12.1 loop as recorded, whose very first Check now exercises TopoGuard's promise that this shape never reaches injection again. Class note for the record: post-modal wreckage is exactly what the Check-gate family exists to prevent — the gates could not catch this shape until TopoGuard existed; now it does, and the residue being cleaned today is the last of its kind that the guard permits.
  > **G12.1 — the placement fix + the topology guard (SHIPPED into the cycle; core + English + tests).** The owner's screenshot delivered the whole diagnosis: Check green, Run dead at *Compile error: Variable not defined* — because the G12.0 **top placement was wrong about VBA module topology.** The include line rode above everything; the splicer replaced it with `hello.vla`'s PROCEDURES; and the emitted module became `Public Sub hello() … End Sub` followed by `Dim vla_step As Long` — **a module-level declaration below the file's first procedure, which VBA silently ignores**, so every later use failed Option Explicit at injection. This is the brief's own hazard list ("module-level declarations below a file's first procedure") in **generated-module form** — the pre-present sweep guards the modules I write, and nothing guarded the modules the machine writes. Check's rows went green because the transpiler had no topology rule: the VLA was legal; the VBA it implied was not. **Fix 1 — placement, corrected on the record:** library imports ride at the **BOTTOM** (`; ---- libraries (G12) ----`, above the carried-macros tail) — topology-safe (procedures after procedures) and *absolutely* source-map neutral (nothing above it moves, even in importing programs — strictly stronger than the L4.2 argument the top placement borrowed). The adopted design's "above main" clause stands corrected here; the placement pin FLIPPED with the design (below the program, positionally). **Fix 2 — `TopoGuard`, the class retired into words:** the emitter now tracks procedure emission per transpile, and a top-level `(dim …)`/`(const …)` after the first procedure **refuses at transpile** — *"…VBA ignores declarations placed after a procedure, and every use of the name then fails Option Explicit…"*, with the library-placement advice in the same breath. Since Check transpiles (G12.0's own hook), this exact incident would now surface AT CHECK, in words, no modal — and so will every raw-VLA program that hand-writes the same mistake. Stated limit inherited by the guard's wording: libraries should carry procedures, not module-level declarations — a lib carrying one now refuses with the rule named. Touched: `VLA.bas` (L13.0 → **G12.1**), `VLA_English.bas` (→ G12.1), `VLA_Tests.bas` (→ G12.1, placement pin flipped + one guard pin added). `VLA_IDE.bas` untouched at G12.0. **Revised deltas: self-test +6 total for G12 (531 → 537)**; counters unchanged; **both goldens still diff EMPTY** (the corpus's declarations all precede its procedures — the guard refuses only shapes nothing existing contains; bottom-matter emits only in importing programs). Re-run the loop with **537 / 0** expected; the five-step maiden story stands, now with step (2) expecting the module to compile and step (5)'s Run expecting **both prints** — and one added step (6): write `(dim stray Long)` as the LAST line of `hello.vla`, Check, and expect the topology guard's words naming the rule, then remove it.
  > **Verification loop:** `VlaDevReload` → Debug → Compile → `VlaSelfTest` (expect **536 / 0**) → Reload (counters line unchanged) → Run `english.txt` → `VerifyReport` (H1:H31 green) → `VlaWriteGoldens` + `git diff` (**predicted: both goldens diff empty**) → **standalone-topology check owed** (`VLA_English.bas` and `VLA_IDE.bas` ship): fresh workbook, built add-in, Check + Run. Maiden smokes, the full G.6 story in order: **(1)** write `hello.vla` beside the workbook — `(sub hello () "waves from the library" (debug-print "from the library"))` — note it carries a P.L6 docstring; **(2)** in the IDE, a two-row program: `Use library "hello.vla".` / `Log "main ran".` → **Check** (expect green rows — and the transpile ran silently); **(3)** `VlaApropos "waves"` (expect `hello - waves from the library (program sub)` — an imported library's procedure, browsable right after Check: the story, live); **(4)** break `hello.vla` (delete the final paren) → Check again (expect the build-error dialog carrying *"…missing ')' for the list opened at hello.vla line 1"* plus the orienting sentence — an include error at CHECK, named); **(5)** restore the lib, add a row `Log "x".` with a raw-row call… simpler: change row 2 to a sentence and **Run** (expect the program to run with the library spliced — `from the library` printing requires a call; optional: add the raw row `(hello)` as row 2 and Run, expecting both prints). Reload the vocabulary after. No reload-list change; no new module.

- **⬜ L.10 — L14: `deflambda`** — the showpiece, deliberately last: it consumes the whole bench.
*(L9 recursive-`cond` and L10 threading remain ✅-REFUSED on Alpha 4 — the refusal ledger; do not re-open.)*

## Bucket 3 — GRAMMAR *(the bench (G5) and audit exist; cheapest-and-purest first; the hard one gated on evidence; definitions on Alpha 4)*

- **⬜ G.1 — G11r: the template-face remainder** — `wrap-text`/`unwrap-text`, `hide-rows`/`show-rows` (and column twins), the `numberformat` family (`format-as-currency`/`percent`/`date`) as carried functors, by exactly G11's fired-only lockstep method; vocab-only, `.vba` golden frozen.
- **⬜ G.2 — G7: slot defaults** `{n:expr=1}` — pure substitution; G1's optional machinery generalized; the audit expands present/absent.
- **⬜ G.3 — G8: number words and ordinals** — bounded token table (one…twenty, first…twentieth), function-word-style, dialect-localizable.
- **⬜ G.4 — G6: list-valued slots** `{r:cell+}` — the translator (a program generator, not a template) substitutes once per matched item into one `(begin …)`; typed validation per item; `+` its own shape class in the audit. The biggest machinery in the bucket; lands on everything above.
- **⬜ G.5 — G9: conjoined predicates** — **stays gated on pilot evidence** (the near-miss log and U5 price it); the scoped same-verb-same-target core is the likely adoptable shape. Do not build ahead of the log.
*(Anaphora stays vetoed; plurals stay answered by G1.1 — both on Alpha 4's ledger.)*

## Bucket 4 — UI *(U-definitions on Alpha 2; the user's microscope first)*

- **⬜ U.1 — U1: Rehearse** — the static "this Run would touch…" preview; its data (typed slots, `EnglishListRefSlots`, provenance) all exists; dangerous verbs first. The users' inspection instrument — first for the same reason `VlaExpand` was.
- **⬜ U.2 — U3: check-on-row-exit + readable error dialogs** — V1's speed, S3's seam; every teaching refusal minted in Bands 1–2 is S3-pinnable and G4-protectable already.
- **⬜ U.3 — U2: Known Sentences categorized/searchable** — S2's most-used pins the top twenty; decide the reader-mode rendering of authoring notation (`{d:left|right}`, bare `into|in`, `[key]`).
- **⬜ U.4 — U5 groundwork: the "that's not what I meant" button** — plumbing here; the button itself ships pilot week one; reuses U.1's summary and S5's trace shape.
- **✅ U.10 — the load-report counters line — COMPLETE (rode L11.1; verified with the cycle at the owner's word; the baseline now prints itself: 102/16/199/5)** *(adopted from an L16-verification finding: the "101 rules / 199 tests / 5 fails / 16 macros" baseline has NO printing source in the current code — it is a recorded composite, not a reload output, and the reload prints only lint + Check timing)*: add a tiny public `EnglishVocabStats()` (rules, carried macros, tests run, expected fails, source file) and print one line from the IDE reload — `loaded: 101 rules, 16 macros, 199 tests (5 expected fails) from spreadsheet.vocab` — so every future baseline paste is one honest copy instead of an assembled memory, and count-drift between passes becomes visible the moment it happens. Companion to U.9 (same load surface — ship together on the next VLA_English pass); the deferred L16 macro-counter check executes the first time this line prints. LOW.
- **⬜ V.1 — `verify:` rows — the self-checking corpus, ADOPTED at the owner's endorsement with the owner's name for it** *(the proposal noted at the L8 adjudication, now business-cased: sanity-checking English macro outputs is a harness users will expect in week 1 — workbook diffing and KPI outlier-flagging are its grown-up forms — and L8 just delivered its engine primitive, so the timing is exactly now)*: program files gain flag-prefixed rows — `verify:` per the owner's naming, which keeps the house `<flag>:` convention and the right connotation — whose content asserts runtime state through the check family. **Design sketch:** v1 recognizes the prefix in the row pipeline, translates the remainder, and tags the step text `verify:` so a failing assertion's dialog names the verifying row — the step-failure infrastructure already carries the reporting; **placement chooses the semantics** (a verify row runs AT ITS POSITION, so end-of-file rows ARE the post-process verification of the owner's connotation, and mid-file rows catch the failing step while state is fresh — both readings, no extra machinery); assertion VOCABULARY is teachable today (a rule like `cell {r:cell} should equal {e:expr} => (check= …)` is one G5-auditioned line, so the English face grows by demand); v2 adds the Check-column annotation and a verified-count summary line. **Difficulty: LOW-MEDIUM for v1** (row-pipeline touch + corpus rows + goldens moving by exactly the new rows + pins), MEDIUM with v2's reporting. **Placement recommendation, deferred to the owner:** ahead of L15 — the macro-stepper is a developer's instrument, `verify:` is the users' harness, and week-1 pressure outranks bench polish; slot it as the next pass after the L8 cycle closes.
- **✅ Design adjudication (owner's question at L8): could VLA_Tests.bas port entirely to VLA? — REFUSED, with the line drawn and recorded.** In theory a surprising fraction is expressible (VLA reaches VBA intrinsics and the engine's publics in-project, traps errors, and now judges with `check=`) — the language is not the barrier. Two walls are: **mechanical** — S5.1: an injected suite cannot inject without wiping itself, so every pin exercising the injection/compilation machinery is structurally un-self-hostable (a program cannot rebuild the factory it stands in); and **evidentiary** — the suite is the gate that decides whether the compiler works, so its verdicts must not be phrased in the language under judgment (a `check=` bug would test itself with itself; the judge does not testify in the defendant's voice). VLA_Tests.bas is the project's BOOTSTRAP layer, the fixed point that lets everything else grow. **The affirmative half, recorded as the porting rule: behavioral assertions port down; structural and evidentiary assertions stay up.** The vocabulary's `test:`/`fail:` proofs already live at the outer layer (199+5 at every load); L8's `check=` rows extend the same pattern to program behavior (Tier-2 probes, user preconditions, phrasebook proofs). **Proposal noted, not adopted: a self-checking corpus** — english.txt-style raw rows carrying `check=` assertions verified DURING the Run, complementing VerifyReport's outside view with an inside one; a candidate for a future bucket at the owner's call.
- **⬜ L.11 — runtime helper docstrings via manifest annotations** *(adopted from the owner's question at the L11 smokes: "where do the (runtime helper)s live, and can they be documented?")*: they live in `VLA_Runtime.bas`, shipped in the add-in, manifest generated by self-scanning that source (sheet tier in built add-ins). The design: a structured comment annotation above each Public helper (e.g. `'@doc: pop an error dialog through the message seam`) that the manifest scanner emits as `name|doc` pairs — the resolve check splits off and ignores the doc, apropos renders it, and the 23 helpers get one annotation each. Touches: manifest builder + both consumers + the sheet-tier cache + runtime annotations + pins; the runtime is shipped payload, so the pass carries a standalone check and byte-drift awareness. MEDIUM — a proper pass, not a rider.
- **⬜ U.12 — an IDE surface for apropos and doc** *(adopted from the L11 fresh-workbook finding)*: the Immediate window resolves unqualified names against the active project only, so add-in publics need `Application.Run` gymnastics from a user workbook — fine for developers, wrong for the users the discoverability was FOR. Give the IDE a "Search the bench" affordance (button or cell prompt) rendering `VlaAproposText` output onto a panel/sheet, and surface `(doc …)` answers the same way — the definition's own "Explain's (doc …) panel" named this. LOW-MEDIUM (rendering and placement; the data seams exist and are pinned).
- **⬜ U.14 — `VlaTryTranspile` instrument** *(adopted at the owner's word after the P.L7 smoke-vehicle incident)*: a small public dev-rig instrument — `VlaTryTranspile text` prints the transpiled VBA, or the error IN WORDS, in the Immediate window; the `VlaTry` doctrine (errors reach words, never modals) applied to transpile-shaped questions. Retires the bare-`?VlaTranspile` modal class entirely, including for include-carrying programs that cannot ride `VlaTry` (top-level forms vs sub-wrapped scratches). LOW — one Sub in `VLA_DevRig` (which cannot reload itself: manual VBE replace FIRST when it ships), a colon-chain's worth of code behind a name, plus its rig-suite line. Slots whenever the owner calls it.
- **⬜ U.13 — a user exploration rig** *(adopted from the owner's bundling question, with the implementation-aware answer recorded)*: the instinct to bundle everything is directionally right, and MOST of the platform already ships — core, English, runtime, IDE are all in the add-in, and users can already teach grammar and macros via vocab files + Reload. What does NOT ship is `VLA_DevRig` (`VlaTry`/`VlaTryValue`/`VlaTimeIt`/reload/diagnostics), and bundling it as-is would be wrong rather than generous: the rig injects scratch modules into `ThisWorkbook` — which inside an add-in IS the add-in (self-modifying artifact), and every injection requires VBE trust access, a setting most user machines have off and many org policies forbid. The user-grade version is a DESIGNED feature: try/eval targeting the USER'S workbook with a trust-detection teaching refusal (zero-cost, worded — the V6.2 doctrine), sweep safety in books the rig doesn't own, and interplay with Undo (see U.11). MEDIUM-HIGH. Until then the honest posture stands: exploration tools are dev-tier; extensibility (the actual goal) already ships.
- **⬜ U.11 — Undo Last Run corrupts state after Run with Trace** *(owner bug report, verbatim repro; occurs in BOTH the dev workbook and a fresh workbook — so the fault is the IDE's own undo logic, not topology)*: load `english.txt`; **Run Sentences → Undo Last Run** produces the expected end-state (Q1 tab removed, empty Output tab); but **Run with Trace → Undo Last Run** removes BOTH Output and Q1 and leaves one blank tab named **`VLAu_Main_Output (2)`**. Reading the name: `VLAu_<tag>_<sheet>` is the undo machinery's own snapshot prefix, and Excel's ` (2)` suffix appears when a copy lands beside an existing sheet of the target name — so the symptom smells like the restore path copying the Output snapshot back WHILE an original still exists (collision → ` (2)`), then deleting the wrong generation, with the trace path diverging from the plain-run path in what it snapshots or registers as run-created. Hypothesis only — the fix starts with a diagnostic pass over the two run paths' snapshot/restore pairing, not a patch. The IDE's own teaching text already knows this failure family ("a leftover sheet … starting with VLAu_ … can cause this"). MEDIUM (undo state machine, two run paths, needs on-machine repro tracing). Not started — recorded at owner's word.
- **✅ U.9 — same-path re-carry teaching fix — COMPLETE (rode L11.1; verified with the cycle at the owner's word; both wordings pinned)** *(adopted from an on-machine finding during L16 verification)*: calling raw `EnglishLoadVocabulary` twice in one session self-collides at the file's first `macro:` line — the loader is ADDITIVE by design (L4 composition; the duplicate guard referees cross-file collisions) and the resetting wrappers (IDE Reload, `VlaTimeIt`) are what made weeks of reloads clean. NOT a bug and NOT an L16 regression: the guard did its job — but its message teaches the WRONG fix for the same-path case ("rename this one" is cross-file direction). Fix: when the prior carrier's path equals the loading path, say "this session already carries this file - reset first (EnglishResetGrammar) or use the IDE's Reload, which resets for you"; different paths keep the rename wording verbatim. One branch, one new pin (same-path message by its words), existing cross-file pin untouched. Rides the next VLA_English-touching pass. Diagnosis trail: set-formula exists ONCE at vocab line 64 (the cited line); registry clears only at init and `EnglishResetGrammar`; IDE 741→746 and rig 729→731 both reset-then-load. LOW.
- **⬜ U.6 — SOP import, the seam + organic numbering** *(owner-proposed this session; adopted after sanity check — PASSES with recorded facts)*: a Word/SOP import reading paragraphs into IDE rows. The sanity check's load-bearing fact: **Word AUTO-numbered lists carry NO number in the text** — the rendered number lives in list formatting, exposed to VBA via `Paragraph.Range.ListFormat.ListString` — so "capture the source list number organically" is one property read, not a parse; manually-typed numbers ARE text and strip by the owner's pattern (`digits + . or ) + space`) at IMPORT, never in the tokenizer (a sentence may legitimately begin with a number). Bonus the design buys free: when a stripped/read number disagrees with the row counter, that is a teaching warning ("line says 7, position says 8 — was a line deleted?"). Difficulty: the strip/capture itself is LOW; the real cost is the import seam (Word COM read, one module, dev-side like the rig) — MEDIUM once, then U.7/U.8 ride it.
- **⬜ U.7 — multi-sentence rows** *(owner-proposed; adopted — PASSES, and the sanity check found most of it ALREADY TRUE)*: the ENGINE already splits and translates multiple sentences per text — `EnglishToVla("Set alpha to 1. Set beta to 2.")` is literally a passing L6 pin — and step tracking speaks per-SENTENCE text while `at-line` carries the source ROW, so N sentences on one row already become N steps all citing that row: exactly the ask, no internal line-splitting, no source-vs-IDE drift. Remaining work: confirm/pin that the IDE row pipeline feeds whole rows unsplit; add the sub-row index to dialogs and Check annotations ("row 12, sentence 2"); near-miss log keyed per sentence. Difficulty: LOW. Hazard recorded honestly: period ambiguity (abbreviations, decimals) belongs to the EXISTING splitter and gains no new exposure here.
- **⬜ U.8 — image rows as counted comments** *(owner-proposed; adopted — PASSES with two adjustments)*: `#` comment rows already exist and are counted by the row pipeline, so `# [image …]` needs ZERO new syntax — the import emits one comment row per paragraph-anchored image and numbering stays aligned with the visual document. Adjustments from the check: (1) embedded images often have no filename — use alt text or shape name when present, else a counter (`# [image 3]`); (2) only images that CONSUME a paragraph become rows — an inline image inside a sentence's paragraph must NOT emit a row or numbering breaks the other way; the import reads `InlineShapes` per paragraph and distinguishes. Difficulty: LOW on top of U.6's seam.
- **⬜ U.5 — S6 (manager half): the phrasebook manager** — *still awaiting the owner's adoption call*; its sequencing constraint is satisfied (G3 shipped), `EnglishRuleSource` is its centerpiece column, and if adopted it lands here with the path seam + `.phrasebook` rename executed before E2 makes external files common.

---

## The ship-sequence ledger *(carried by reference, unchanged: Alpha 4 Bands 1–4 / Alpha 2 definitions)*

D2 freeze → U1/U2/U3 (now bucketed above) → E2 first-contact (starter phrasebooks authored via G5, multi-phrasebook loading on G3) → M2a/M1a/F3 → D3+M5b → E1 → **E3 pilot (the gate, kill criteria binding)** → F1 interpreter ∥ F2 LLM bridge → U4/Tier-2 helpers → the Tier-2 ladder → M2b+horizon. The buckets above are the engineering that makes each of these cheaper when its turn comes; the pilot's log re-ranks everything, G9 explicitly included.

*Standing rules in force: no optimization without V6's before-number; no checkmark without the owner's word; goldens diff empty unless the record predicts otherwise, and an absent predicted diff is as much a finding as an unpredicted one; every pass ships with its verification loop, its deltas from the printed baseline, and its record on this ledger.*
